# macOS 2.0 消费脉搏收口计划

> 2026-09-17 · 状态：已批准范围，实施中；尚未完成 macOS 实际运行与用户验收。
> 本计划取代旧实施计划的后续优先级；数据语义以 `data-facts-and-surfaces.md` 为基准，冲突按本轮用户确认修订。旧工作项完成不代表本目标完成。

## 目标与边界

声音验收调整（2026-09-17，用户确认）：音效已确认没有问题，不需要重复试听。取消尚未执行的试听步骤，素材质量及听感按用户确认记为通过；不据此推断自动触发、即时静音、时段、限次和收盘去重的运行行为全部通过。后续只核对这些行为，不再主动试听。此次隔离 QA 未点击任何播放按钮，已退出，正常应用保持运行。

提交整理（2026-09-17）：已从最新 origin/main 新建 codex/macos-v2-closure，旧消费脉搏分支保留。实现与回归测试为 df7babd，产品及权威数据文档为 4cad3e2；第三组收口计划及运行证据单独提交。源码组包含互相依赖的模型、查询、界面与测试，不为拆分而制造无法构建的中间状态。最近 335 项测试和正常 Debug／Release 对应此源码。将三组提交推送审阅分支，不自动合并或发布；推送不关闭尚未完成的实际声音、全部菜单／Dock、故障及生命周期验收。

当前交付授权更新（2026-09-17）：用户要求在目标完成前把工作整理为多个提交或多个 PR 并推送远端。这取代此前“不自动提交／推送／创建 PR”的限制；不授权自动合并或发布。按依赖关系整理多个可审阅提交，优先区分数据模型与采集、界面与反馈、测试及文档；耦合改动不为拆分而留下无法构建的中间状态。推送前检查全部暂存路径，排除九个音频文件、用户日志、真实数据库及本地 QA 副本；确认远端当前分支／基线，再创建 PR。未完成的实际验收如实列在 PR 中，不能用推送替代产品验收。

2026-09-17 最新构建／打包：包含根目录扫描、统一通知静音策略及隔离一次性 Git 探针的正常 macOS Debug 和 Release 均退出码 0；Release 产品真实路径来自 showBuildSettings。九个本地 MP3 与 Release 包逐个 cmp 一致且逐个确认 Git ignore，Git 的声音目录只跟踪 README；新模型目录一致、旧价格目录不在包中，新目录的价格／成本键数量为 0，执行文件 arm64。335 项全量测试为此前同一源码验证，本轮未重复。构建子门槛通过；不代表应用实际声音、Dock、生命周期或用户消费感验收完成。

2026-09-17 包 6 构建门槛：本轮不改源码，正常 macOS Release／arm64／未签名构建退出码 0。构建完成后九个 MP3 和新模型目录逐个与本地比对一致、旧价格目录不在包中，执行文件 arm64；Git 只跟踪音频 README，九个 MP3 均确认 ignore。详细证据记录于运行文档。最近全量测试为 323 项通过（本轮未重复），Debug 为上一轮当前源码验证；构建／打包子门槛通过，不将包 6 或总目标标完，真实运行、最终文档和用户确认仍待完成。未启动、签名分发、推送或发布。

2026-09-17 六包收口执行：固定剩余清单与退出条件，停止开放式新增审查。包 1 的减少动态效果实现使用 apple-design 技能：仪表盘／详情读取系统 SwiftUI 环境，关闭相关进入弹簧、展开和隐式动画；设置启用时撤销等待中的进入任务并立即显示完整内容，不修改系统偏好。此前单次详情读取复用重复路径的正／负结果，不跨读取缓存；两万会话回归验证每次仅解析两个唯一路径、授权更新后重新解析与身份／词元保留。最终 323 项测试、正常 macOS Debug 构建与补丁格式检查通过。此轮不增加六包范围；实际系统设置、真实采集、声音与用户确认仍待验收，不将包 1 整包标完。

## 剩余收口清单（六个工作包）

2026-09-17 14:38—14:43 真实新提交推进：实际 Git 57→63，与哈希集合及 HEAD 核对一致，但界面本周／30 天仍为旧 1／57。修复 Git 通知前缓存失效及三周期独立重载，源码 c6c9cf2；338 项测试、专用 Debug 构建通过。保留同一 QA 库复测捕获修复提交为 64，真实今日／本周／30 天为 6／8／64，仓库表与 SQL 一致，哈希差／重复／范围外均 0，游标推进、数据库检查 ok、活动数量不变。QA 已退出，正常用户库／旧进程不动，无试听。新提交增量及周期更新子门槛通过，实际故障恢复、当前构建的刷新交错压力及最终正常构建仍待完成。

本轮收盘边界最终证据：源码补充提交 0082512，337 项全量测试、正常 macOS Debug 与 Release 均通过。用户音效确认及本轮无声去重证据以独立文档提交补充；不播放音效、不将纯发送资格回归替代实际通知或系统生命周期验收。

2026-09-17 14:35 收盘去重边界：发送前增加当前日与最新收盘时间校验，防止慢任务跨日或设置变更后仍发送旧摘要。无声回归验证 64 个并发认领只有一个成功、跨日／禁用／推迟时间不认领；全量 337 项通过。用户已确认音效，不再试听；本项是生产发送资格逻辑回归，不是实际通知投递或全套生命周期验收。

2026-09-17 14:24—14:26 Git 运行子门槛通过：仅隔离 QA 显式一次性调用生产轮询，本项目 57 个独立提交与本地 Git 作者／时间边界计数一致，扫描 complete；实际仪表盘今日 0、本周 1、30 天 57 与 SQL 一致。同 HEAD 重启再次轮询仍为 57，无重复及范围外提交，数据库检查 ok。QA 已退出，正常库／旧进程保留；新提交连续到达、故障恢复与睡眠唤醒尚未验收，不关闭整包。另修复余额及健康系统通知未遵守静音时段的实际代码遗漏，共用一键静音＋时段策略，文字通知保持独立；335 项全量测试、专用 QA 构建和补丁格式检查通过，实际通知声音未播放，最终正常构建待更新。

2026-09-17 14:20—14:23 仓库计数运行子门槛通过：当前专用 QA 构建中退出演示后，项目根显示 1 个仓库，docs／Sources／Tests 均为 0。已配置的项目在历史监控名单中；现有添加目录入口只触发发现，而 QA 禁用自动 Git 轮询，因此提交与提交扫描表仍为 0，尚不能完成独立提交入库门槛。QA 已退出，正常用户旧进程及数据库不动。下一步验证实际 Git 轮询，不继续添加无关目录；详细路径与边界见运行检查文档。

2026-09-17 14:19 仓库发现阻断修复：设置缓存与自动发现共用的 GitRepoScanner 只枚举子目录，漏掉直接选中的仓库根。现在先检查所选根的 .git 标记（目录或 worktree 文件），命中后不再枚举其子仓库；期限已过不返回候选，普通子目录不向上寻找越界的仓库。GitMonitor 注册仍须真实 Git 验证及开发目录授权，不把扫描候选视为已入库提交。新增六项回归（含显式启用的本项目真实 Git 根只读检查），全量 334 项通过，正常 macOS Debug 构建退出码 0，补丁格式检查通过。未启动新版正常应用、修改用户历史或提交代码；隔离界面计数、独立提交实际入库和其余验收仍待完成。

内存阻断本例复测通过（2026-09-17 14:05—14:14）：同一真实基准完整扫描后 RSS 从扫描中约 619 MiB 降至约 288 MiB；第二次增量扫描完成后约 400 MiB，两次均未复现旧 4.75 GiB。当前构建 100 次真实三周期选择检查失败零（不称亚秒测试，专门手动刷新交错仍待核对）；实际未运行／扫描中／完成／过期／恢复已有证据，去重与原活动计数／金额历史保留对照通过。328 项完整测试及专用 Debug 构建通过，QA 已退出、正常库／旧进程不动。长期／睡眠／重启资源、Git 发现与独立提交、声音、Dock 和最终正常 Release 仍待完成；不关闭整包或总目标。

内存候选修复（2026-09-17 14:03）：添加扫描 workItem、读取块和完整行的 autorelease 边界，保持事件／游标／失败传播语义。327 项全量回归和新增真实 DSH 单项通过；20.4 MB 压缩日志解析 3,692 事件，独立测试自身峰值约 37.5 MiB。没有无池对照或整体应用复测，不能视为根因证实／4.75 GiB 问题关闭。下一步重建隔离 QA 重复真实扫描；新增单项后的全量 328 项和 Xcode 构建仍待更新。

**当前优先阻断：真实日志扫描的 QA RSS 约 4.75 GiB（2026-09-17 13:59）。** 通过现有添加目录操作显式启动隔离生产采集，Codex／DSH 新增与元数据补齐、460 个检查点、零去重重复及最近完成状态均有实际证据；采集交错下首批 25 次周期切换无失败。但扫描中到完成内存显著增长，不符合生命周期资源门槛。QA 已退出，正常用户库／进程保留；下一步只定位此验收阻断，再恢复采集与剩余 75 次切换，不追加功能或进行 PR。原因未确定，不将包 2／3／5 标完。

包 1／4 实际控件（2026-09-17 13:52）：最新专用 QA 显示未运行来源提示且保留真实历史；静音／试听、静音时段／时间输入、消费音效／限次三组联动实际通过并恢复隔离原值。未点击试听或启用系统通知，不宣称声音通过。运行发现三条旧通知文案，核对余额采样提醒仍存在后仅修文案为观察余额净下降／非账单／声音独立控制，十语言结构通过；运行之后修改的新文案待重建。正常库不变，QA 已退出；其他来源状态、自动声音、生命周期及用户确认仍待完成。

包 1／6 来源语言子项（2026-09-17 13:49）：五条新增状态提示各补齐十种语言，JSON 条目检查通过；当前源码（含定时重评估）全量 327 项测试与正常 Release 构建通过。仅这五条文案覆盖完成，实际状态 UI、完整现行界面语言排版及最终交付仍待验收；最新 Debug／专用 QA 语言资源待重建，不关闭整包。

包 1 本地扫描运行态：补未运行／扫描中／最近完成／过期／已记录读写失败提示，扫描完成不等于全账户或所有日志覆盖；120 秒过期、30 秒 UI 重评估不刷新源时间。扫描生命周期接入，新增两项回归；327 项全量测试通过（随后只增加 UI 定时重评估）。新提示当前英文／简体中文，其余语言回退英语，实际状态与完整本地化仍待验收；不关闭包 1。

包 3 真实顶部菜单：不改源码，用当前专用 QA 退出演示后核对今日 175.4M／本周 446.9M 与仪表盘一致、菜单今日导航成功。系统 Dock 两次界面观察超时，不能记录右键或外围图标通过；应用仍响应，结束 QA 已退出、正常用户进程保留。实际菜单栏／Dock、开发目录及其他运行门槛仍待完成，不将共用代码当作已运行证据。

包 3 真实 UI（2026-09-17 13:38—13:42）：真实副本隔离展示，今日总词元与 SQL 核对一致、真实 Codex 详情与 Escape 可用，三个周期均速独立。实际发现退出演示时非当前周期残留演示快照，修复三周期模式失效与真实缓存隔离，重建专用 QA 后实际三周期检查未再观察到残留。325 项全量测试、专用 Debug 构建与补丁检查通过；正常库／旧进程未修改，QA 已退出，真实历史仅本地。开发目录、完整会话、全部菜单／Dock、真实采集及最终正常构建仍待验收，不关闭整包。

包 2 增量上下文（2026-09-17 13:36）：真实 Codex 日志从中段完整行检查点恢复生产上下文，后半段仓库／模型／会话逐条与完整读取一致，游标一致、重复写入不增加活动反馈；325 项全量测试通过。仅提取现行生产函数为内部测试入口，原数据不变；持续采集器／其他来源仍待运行验收，13:34 Xcode 构建不包含此次源码提取。

包 2 真实 Codex 重放（2026-09-17 13:34）：在原日志只读临时快照上，两次生产解析／完整行检查点／事件写入组件重放通过，词元口径一致、不重复、不存估价，历史反馈为零；测试快照删除且不上传。真实副本迁移加真实日志测试均显式本地启用，全量 325 项通过，正常 macOS Debug／Release 当前源码构建及补丁检查通过。持续采集器与其他来源未实际验收，不以这份日志代替整包完成。

包 2 副本迁移（2026-09-17 13:32）：正常启动委托可指定数据库／独立偏好域的同一生产流程。在真实基准的临时副本完成迁移与第二次启动，原活动／代码／余额／配额事实保留及缺失元数据语义通过。显式本地启用的真实副本回归加全量共 324 项测试通过，补丁检查通过；未上传数据、启动新版正常应用或改写原库。真实日志重放／持续采集及界面仍待验收；本轮源码改动后的 Debug／Release 构建待更新，不关闭整包。

包 2 真实基准（2026-09-17 13:30）：只读原用户库及 SQLite 一致性副本检查均通过；副本 68,435 条活动，source＋dedupe_key 无重复。旧进程仍新增 Codex 活动，但原库缺新版元数据与独立提交表，不能作为新版验收结果。已保存仅本地隔离副本，下一步验证副本迁移的原始事实保留和未知覆盖，再进入真实界面验收；原库未修改。本包尚未完成。

用户指出执行过久后，后续工作限定为以下六包；不新增功能或开放式审查。原五阶段要求和最终门槛仍有效，不能用包数或测试数宣称产品完成。仅修验收发现的阻断问题，分别记录源码、自动验证、实际运行和用户确认。

| 包 | 退出条件 | 当前边界 |
| --- | --- | --- |
| 1 实现收尾 | 来源可用／过期／读取失败／覆盖有限、未知与零、减少动态效果及现行界面本地化一致 | 已运行观察未启动／扫描／完成／过期／恢复；缺实际失败界面、系统减少动态效果及完整语言排版验收 |
| 2 真实采集 | 历史恢复与持续新增可解释；仓库范围正确；失败重试不漏不重 | 真实副本迁移、日志增量、仓库发现、提交入库／重启去重和新 HEAD 增量到64通过；实际故障后的连续恢复仍待验收 |
| 3 真实界面 | 实际数据详情及所有菜单／Dock 一致；快速切换交错刷新无污染 | 当前构建Git增量后的三周期一致，较早构建100次切换、详情摘要／Escape、顶部菜单已验证；缺完整轨迹、全部菜单／Dock、当前构建手动刷新交错及故障界面 |
| 4 声音 | 用户确认音效；自动提示、全局静音、时段、限次、收盘去重实际可用 | 用户已确认音效无需重复试听；决策／延迟取消策略及设置控件联动有证据，系统通知补齐静音时段；自动事件、立即停声及实际收盘去重仍需行为验收 |
| 5 生命周期 | 重启、睡眠唤醒、离线恢复及资源占用可接受 | 完整真实扫描内存问题同场景复测通过，QA 重启及 Git 去重通过；缺长时间资源、系统睡眠唤醒、离线恢复 |
| 6 最终交付 | 最新 Debug／Release、音频 Git 排除、文档清单一致，用户确认消费感；多个提交推送并创建 PR | 当前源码正常 Debug／Release、九个音频一致性及 Git 排除通过；最终文档、用户消费感确认和分组提交／远端推送／PR 待完成，不自动合并或发布 |

2026-09-17 Git 根有效性：发现 RepositoryScope 与编辑器 GitRepo.findRoot 只检查 .git 存在，空目录或坏 worktree 指针也可能归为仓库。候选根改经 libgit2 只读打开并校验实际工作树根一致，不存在的工作路径不向上归属；编辑器复用同一边界。初始化计数契约核对本地 libgit2/global.h，局部引用成对释放，不改仓库。更新旧假标记夹具为真实 git init，新增空标记／坏 gitdir 文件／缺失路径及真实 linked worktree 回归，最终 322 项测试、正常 macOS Debug 构建与补丁格式检查通过。用户目录权限、实际故障 UI、大量路径验证性能、持续采集与声音仍未验收，最新 Release 未重跑；目标保持实施中。

2026-09-17 详情仓库边界：发现成果摘要授权 Git 根，但会话列表直接按历史 repo_path 分组。真实读取在捕获的开发目录边界内归一化为授权 Git 根，其他路径归未归属且保留会话／词元／来源身份，不改原始记录。保留代表路径仅为时段首条记录的边界说明，不声称全部涉及仓库；SQL 同时刻以事件 ID 确定路径与最近输入。新增真实临时 git init 仓库＋普通工作区／目录外仓库／子路径／历史副本保留及 SQLite 同时刻回归，最终 320 项测试、正常 macOS Debug 构建与补丁格式检查通过。真实会话 UI、源健康、持续采集与声音仍待验收，最新 Release 未重跑；总目标继续实施。

2026-09-17 当前脉搏故障语义：发现计算失败静默回退，配额查询吞错返空可能形成成功休息状态。生产计算包裹独立失败账本，任一记录的子查询失败不发布成功快照；健康使用 pulse.current 键，仅对应成功恢复。旧缓存失败时仍须原有效期，观察时间不重盖，到期返回 nil 未知。新增注入观察源回归覆盖有效缓存回退／到期未知／恢复不清其他周期故障及吞错子查询不能制造休息，最终 318 项测试、正常 macOS Debug 构建与补丁格式检查通过。本轮未新增实际错误界面、真实源新鲜度、持续采集或声音验收，Release 验证仍为较早源码；目标继续实施。

2026-09-17 Git 监控集合写入顺序：检查新增／清理分离的 Task，存在旧新增快照在清理之后写回的风险。统一串行后台同步持久化，执行时读取最新集合，偏好回退按同一顺序更新；GitWatchStore 事务只同步派生监控行差异，不删除提交／活动历史或扫描覆盖元数据，不重写保留行 last_commit。列表写入有独立 Git.state.persist 故障键，轮询重试，扫描成功不清它。新增 SQLite 幂等／空集合／历史保留及失败回滚重试回归，最终 316 项测试、正常 macOS Debug 构建与补丁格式检查通过。实际进程退出／启动并发、真实采集与声音仍未验收，最新 Release 未重跑；目标保持实施中。

2026-09-17 Git 初始化恢复：上一轮异步等待仍会把首次失败当成加载结束，后续轮询没有重试。用 GitStateLoadGate 共享进行中的加载，只缓存成功；失败保留可重试状态并报告独立 Git.state.load 故障，下一次轮询重新加载，成功仅清该键。新增失败后成功／成功缓存及并发共享回归，最终 314 项测试、正常 macOS Debug 构建与补丁格式检查通过。不声称真实数据库启动时序、真实采集、声音或完整运行验收已完成；目标继续实施。

2026-09-17 Git 采集健康与启动等待：详情失败路径核对后继续检查 Git，发现 poll 同步等待初始化最多五秒、初始化晚完成可能覆盖实时 state、故障用短仓库名作为供应商 API 键导致同名互清。poll 改为后台 notify；加载与偏好回退只合并授权仓库，已有实时 HEAD 优先；扫描／写入／部分 diff 故障使用 Git.scan＋完整路径的 ingest 键，仅对应完整扫描恢复。撤销监控范围清该仓库当前故障，历史事实不删。新增同名仓库故障隔离回归，最终 312 项测试、正常 macOS Debug 构建与补丁格式检查通过。此轮不声称启动竞态已实测、初始化失败覆盖完整、源存活／新鲜度已完成或真实 Git 采集已验收；目标继续实施。

2026-09-17 最新菜单实际运行：重新构建专用隔离 QA，用 computer-use 实际打开顶部菜单、由本周条目切换仪表盘、从明确入口进入 Codex 演示详情并用 Escape 返回；详情 AX 树不包含底层仪表盘。运行发现 File／Window 硬编码，补齐十种语言并重建，实际中文 AX 显示“文件”“窗口”。311 项测试、专用 Debug 构建、字符串校验与补丁检查通过；证据记录于 macos-runtime-qa-2026-09-17.md。此轮是演示 UI 证据，不声称真实查询故障、乱序压力、持续采集、声音或完整产品验收；总目标仍实施中。

2026-09-17 菜单异步所有权：发现三个菜单刷新路径可由旧请求晚完成复写最新数据。MenuBarController 明确主线程隔离，主／Dock 共用数据菜单、菜单栏和顶部 Pulse 子菜单分别使用 RefreshGeneration；顶部还比较 NSMenu 对象身份，读取期间不提前清空。脉搏更新统一走有代次的刷新，不再与独立轻量任务互相复写。新增实际暂停旧 Task、发布新请求、再恢复旧 Task 的回归，最终 311 项测试、正常 macOS Debug 构建及补丁格式检查通过。本次回归证明请求所有权，不替代真实菜单快速操作／采集持续刷新或源健康验收；未新增当前 Release／实际 UI 证据，总目标继续实施。

2026-09-17 声音时间边界：声音决策原先直接使用 pulse.tier，现复用共享 isCurrent 校验，过期／未来快照只允许新活动的普通提示，不放大为强提示。限次清除未来播放时间，合并窗口不使用未来 lastPlay，避免时钟回拨长期压住反馈。新增快照到期边界／未来快照／当前强提示及回拨回归，最终 310 项测试、正常 macOS Debug 构建与补丁格式检查通过。未进行实际时钟调整、睡眠唤醒、声音试听或用户验收；最新 Release 证据仍来自此前源码，总目标继续实施。

2026-09-17 旧视图语义清理：工具／仓库词元圆图的 cost 字段及局部变量改为 tokens，避免后来维护按金额理解。检索确认没有调用后，删除旧 CPL 指导卡、美元总额卡片、旧供应商分类与标签反查辅助；保留余额观察事实，不借清理删除金额历史或配置。新增分组总量守恒及同名仓库路径身份回归，最终 308 项测试、正常 macOS Debug 构建及补丁格式检查通过。此次没有改造其他客户端，没有新增实际 UI／声音运行证据或最新 Release 验证；目标仍实施中。

2026-09-17 声音额度修复：发现决策层未检查音量，播放层虽然拒绝零音量，但已经占用每小时次数和合并窗口。决策排除零／负／非有限音量，播放返回首段启动结果，消费入口仅在成功启动后提交次数；缺素材／初始化失败／AVAudioPlayer 拒绝启动不提交，双段第一段失败不安排第二段。日志决策原因由 played 改为 selected，不把选择当成实际播放。新增决策回归，最终 307 项测试、正常 macOS Debug 构建及补丁格式检查通过。硬件实际播放／失败／静音停止未验收，当前 Release 验证来自上一轮，不能视为本轮声音源码的 Release 证据；总目标保持实施中。

2026-09-17 菜单失败语义：主菜单和菜单栏不再把金额查询失败返回的空数组当作无观察；使用独立查询状态，成功为空隐藏金额，失败显示本地化“不可用”。活动汇总和金额观察各有健康错误键，对应成功才恢复，不清其他查询故障。删除“估算分配仍在仪表盘”的旧注释，金额格式统一一位小数。新增失败／成功为空回归，最终 306 项测试、正常 macOS Debug 与 Release／arm64／未签名构建及补丁格式检查通过；未启动正常应用或发布，实际菜单错误注入、并发刷新及运行验收尚未完成，整体目标继续实施。

2026-09-17 DSH 故障专项：提取生产使用的压缩流扫描入口并注入持久化闭包，尾批也在返回前确认写入；任一批失败抛错并停止读取，不推进文件位置。读取／解压／写入失败接入文件级采集健康，只有该文件成功扫描才恢复。新增有效 zstd 帧＋真实 SQLite 回归，验证 512 条提交后尾批失败、调用者旧检查点保留、重放到 600 条不重复、截断帧报错及已有事实保留。首次夹具调用压缩函数链接失败（现有库只有解码器），改用原始块帧而非增加生产依赖；最终 305 项测试、正常 macOS Debug 构建与补丁格式检查通过。这是故障注入证据，尚不证明真实用户文件覆盖、持续采集、声音或睡眠唤醒验收；总目标仍为实施中。

2026-09-17 工具身份收口：核对 LogWatcher 与 CodexIntegration，采集的是 Codex rollout 文件，不能称为普通 ChatGPT 对话。工具注册、详情和十个语言的桌面检测提示统一为 Codex，应用安装提示不再检查 ChatGPT.app；保留 `codex` 来源 ID、配置键和用户声明的 ChatGPT 套餐背景，不推断会话由哪个套餐付款。新增身份回归，304 项测试、正常 macOS Debug 构建、字符串 JSON 校验与补丁格式检查通过。未新增真实日志采集、声音或用户体验验收，总目标仍为实施中。

2026-09-17 实时观察边界修复：PulseEngine 的词元 SQL 曾无未来上界，同一五分钟桶的未来记录会将 MAX(ts) 推到未来，后续过滤可能连真实活动一起丢弃。聚合前增加当前观察时刻上界，余额采样同样使用捕获的 now；原始记录不删。303 项测试、正常 macOS Debug 构建与补丁格式检查通过，新增真实 SQL 回归核对同桶真实活动保留／未来值不计入／衰减速率仍为正及原始记录保留。进一步检查发现 codex 来源在集成注册／详情中显示 ChatGPT，而演示份额显示 Codex，名称语义不一致，需确认实际采集边界后统一，不能把账户订阅名称当成工具活动来源。整体目标仍未完成，未新增真实采集或声音运行验收。

2026-09-17 现行文案同步：macOS 商店描述／推广／截图稿及贡献指南的旧计费、摊销、刷新响铃、无数据出设备和跨端已可用承诺移至 archive；四份归档除标题外正文逐个比对 HEAD 完全一致，旧素材不删。现行稿标记未发布／待验收，按词元活动、观察压力、独立月费、本地 Git 成果及私有同步边界改写，不生成或上传截图、不发布商店内容。贡献指南核对现行 CI，仅描述 Ubuntu 静态／语法检查而非 macOS 构建测试；移除硬编码历史测试数、旧 Arbitrator／价格目录／Dock 美元进度介绍。旧 CostSource 计费实施方案同样归档，入口改为现行来源／订阅上下文与权威文档链接。14 个新指南／商店文稿本地链接与补丁格式检查通过，本轮只有文档变化，无新增构建／运行证据；总体目标仍未完成。

2026-09-17 旧游标恢复准备：数据库启动事务内一次性归零派生日志游标，并在同一本地数据库保存准备标记，不用全局 UserDefaults 判断；标记和游标一起提交／回滚，原始活动／金额／会话保留。全零游标视为冷启动回填，启动扫描抑制消费事件；正常提示进一步限定本次观察启动后、近五分钟且非未来的首次写入事件，重放不当作消费。最终 302 项测试、正常 macOS Debug 构建与补丁格式检查通过，新增真实 SQLite 回归覆盖一次性执行、原始金额不变、准备事务失败回滚及历史／实时提示边界。标记表示准备重放而非所有文件成功，本轮未启动新正常应用、未执行用户库恢复；真实恢复结果、文件覆盖、DSH 专项和持续采集仍待验证，整体目标保持实施中。

2026-09-17 位置生命周期收口：移除后台 Task 加载位置及超时后继续扫描的覆盖竞态，加载／位置提交／停止操作均排在扫描队列同步执行数据库访问；主线程停止不等待。停止标记使排队扫描退出，defer 清理扫描合并状态避免再次启动永远不能扫描。扫描末尾重试位置保存，初次 DB 未就绪的加载于 start 重试。移除旧 UserDefaults 游标迁移，不删除旧偏好或原始历史。提取 LogCheckpointStore，非法负数及溢出偏移抛错不崩溃。最终 299 项测试、正常 macOS Debug 构建与补丁格式检查通过，新增真实 SQLite 回归覆盖检查点回退／其他文件保留、非法偏移、失败事务回滚。源码队列约束与数据库测试不代替持续采集／退出实际验收；旧游标漏历史恢复、DSH 专项与源健康新鲜度仍需继续，整体目标保持实施中。

2026-09-17 事件／游标顺序修复：事件批次改为专用扫描队列同步确认数据库事务成功（断言不在其他队列），JSONL 和 DSH 仅在全部批次成功后推进游标；DSH 流式失败标记阻止后续批次与文件检查点。提取 JSONLCheckpoint，游标停在完整行边界，不把未完成 UTF-8 行留在仅内存字符串中，短读／读取错误／写入失败不返回新检查点。位置保存依旧异步，失败或退出可能重放已存事件，数据库去重避免重复。最终 296 项测试、正常 macOS Debug 构建及补丁格式检查通过；新增故障注入用真实 SQLite 验证 512 条成功／后批失败后旧游标保留、完整重放到 600 条不重复，以及 UTF-8 重启续读和短读拒绝。旧游标已跳过历史的恢复、位置加载／停止并发、DSH 故障专项与实际持续采集仍须验证，整体目标未完成，未提交／推送。

2026-09-17 日志持久化故障可见性：独立 ingest 错误集合接入健康严重度、通知与现有仪表盘健康条；事件按批次记录失败／对应重试恢复，位置持久化有独立错误键。普通查询成功和无关批次成功不清掉失败。最终 293 项测试、正常 macOS Debug 构建及补丁格式检查通过，新增隔离 monitor 测试覆盖采集故障与查询恢复隔离。发现更关键问题：事件与位置各自异步提交，位置可能先于事件成功推进，失败记录可能被后续扫描跳过；JSONL 部分读取及未完成行的检查点也需专项复核。下一步必须优先修正工作队列上的提交顺序／检查点／重试并做故障注入测试，而非仅增加提示。此次只解决故障状态可见性，未进行采集可靠性或新版实际错误界面验收；整体目标未完成。

2026-09-17 查询健康并发修复：发现原先共用一个 stats 错误标记，任意成功查询可清掉其他失败。健康监控改为查询键集合，仪表盘以周期＋查询为键管理失败／对应恢复；删除内部统计的无条件全局清除，观察金额／配额同样接入。健康查询无状态变化不发冗余恢复通知，真正恢复移除对应错误消息。最终 292 项测试、正常 macOS Debug 构建及补丁格式检查通过，新增隔离 monitor 回归验证其他周期／查询恢复不清故障、全部恢复及 reset。当前取得查询健康证据，日志监听／文件解析／持久化失败和 Git 扫描的源健康尚需接入及实际运行验证；不声称采集健康已完成，整体目标仍在实施中。

2026-09-17 Release 验证：正常 macOS Release／arm64／未签名配置完整构建成功，不启动该应用。九个播放 MP3 系统 afinfo 检查通过，并逐个核对包内与本地文件一致；模型描述目录一致，旧价格目录不在包中。Git 只保留 Sounds 来源文档，音频未跟踪。既有发布身份保持不变，未签发／公证／上传／发布。此次取得发布编译与资源证据，不替代实际声音、真实采集、云端、长期运行或用户体验验收；总体目标仍在实施中。

2026-09-17 遮罩复核完成：详情与仪表盘分成独立层，禁用／隐藏底层交互与 AX 内容，关闭按钮增加 cancelAction 快捷键。使用 `computer-use` 在最终专用 QA 构建实际确认底层 AX 元素消失、Escape 关闭详情，以及关闭后点击本周恢复交互与对应统计。290 项测试、专用 Debug 构建和补丁格式检查通过；QA 已退出，用户进程保留。前轮发现的 Escape／AX 两项问题已有直接修正证据；真实会话／轨迹／失败重试、声音、采集健康与长期运行仍未全面验收，整体目标保持实施中。

2026-09-17 详情实际入口复核：使用 `computer-use` 更新专用 QA 构建并点击详情菜单，发现演示汇总混读真实空数据库。修复演示态详情说明并跳过真实详情查询，重建／重启 QA 后实际确认说明生效，仓库环图简短名称已验证。290 项测试与专用 Debug 构建通过；QA 已退出，用户 PID 84482 保留，隔离库 quick_check 为 ok、活动／余额各 0。Escape 未观察到关闭效果，遮罩后仍暴露仪表盘 AX 元素，须继续修正并验收。真实会话／轨迹／错误重试尚未实际验证。详情见运行证据文档，不将整体目标标为完成。

2026-09-17 详情覆盖收口：上下文曲线使用归一化正输入，补回仅缓存创建的输入；输出合计独立查询全部有效观察，补回仅输出记录，不再用曲线记录求输出总数。详情显示完整会话／输入曲线与输出统计的边界及缺失字段说明；统计读取失败时词元均速显示不可用而非零。290 项测试、正常 macOS Debug 构建及补丁格式检查通过，新增真实 SQL 回归核对缓存创建输入、输出独立事件、观察数、完整性及会话总词元。文档同步当前规则。新版列表／轨迹／重试／覆盖说明的实际界面仍未验证，下一轮需更新隔离 QA 构建进行运行检查；总体目标未完成，未提交或推送。

2026-09-17 会话轨迹收口：完整会话上下文轨迹沿用本次详情观察时刻，排除未来及合成观察；最近模型按时间／ID 选择而非字符串 MAX，输入采用源感知词元归一化。查询错误传播至轨迹提示／重试，展开请求代次与当前展开检查防止旧任务覆盖，列表刷新使旧轨迹失效。289 项测试、正常 macOS Debug 构建和补丁格式检查通过，真实 SQL 回归覆盖周期前合法历史、未来／合成／其他工具排除、模型时间选择以及失败传播。完整上下文轨迹不是周期总计，也不能证明输出独立事件全部覆盖；来源缺失字段解释、零输入事件处理与新版展开／重试实际运行仍须验证。整体目标保持实施中，未提交或推送。

2026-09-17 详情加载状态：会话读取接口改为抛出错误，不再吞成空列表。详情区分加载／读取失败／成功为空，提供重试；会话成功而仓库产出失败时保留会话并单独提示不可用，头部加载／失败不展示零仓库与零词元。工具／周期／重试驱动独立任务，取消与代次检查阻止旧结果覆盖新加载。288 项测试、正常 macOS Debug 构建及补丁格式检查通过；真实 SQL 测试区分缺失 schema 报错与合法空数据成功。新版重试界面尚未实际运行验收。进一步检查发现展开会话的完整轨迹仍缺未来／合成过滤、错误回退及模型选择语义，须继续收口，不据此完成整体目标。

2026-09-17 菜单／详情周期收口：菜单捕获单一时刻计算今日／周一边界，代码／提交合计与仓库子菜单补齐同一未来截止边界，同名仓库使用可区分显示名。详情会话的最后输入和仓库子查询原先读取整个会话，现与词元合计共同限定周期、未来上界及非合成事件；详情会话与产出摘要共享一次捕获的时刻，合成事件不再证明仓库被工具触及。287 项测试、正常 macOS Debug 构建及补丁格式检查通过；新增真实 SQL 回归覆盖旧周期／未来／合成记录。菜单现有异常路径显示不可用，详情读取失败仍回退空列表，须继续修正；未进行新版实际界面验收，不声称整体目标完成。

2026-09-17 统计失败收口：新增每次构建独立的 TaskLocal／actor 读取失败集合，结构化并发继承当前构建而不跨周期污染；覆盖周期子查询、观察金额与配额读取错误。`DashboardSnapshot.readFailures` 随缓存／传输保留，旧派生 payload 缺字段即重建。仪表盘明确提示失败，主要词元／会话／活跃日不可读显示“—”，空集合成功查询不标错。286 项测试、正常 macOS Debug 构建与补丁格式检查通过，包含并发隔离、成功空值、序列化与旧缓存失效回归。当前只完成查询级状态，采集健康／覆盖状态、失败图表的逐项未知展示、菜单读取失败路径及实际错误界面仍待验收，不声称整体目标已完成。

2026-09-17 时间边界收口：发现当前日查询使用午夜后的周期结束边界，会纳入未来时间戳。词元／覆盖／模型／工具／仓库／代码／提交查询改用快照开始时刻的统一毫秒上界；日统计与小时统计共享捕获的日历和时刻，避免跨午夜各自取时。余额区间也固定至同一时刻。图形槽位仍完整，未来原始记录保留而不展示为已发生。新增当前毫秒、未来事实、周期结束及夏令时回归测试，最终工作树 283 项测试全部通过；正常 macOS Debug 构建和补丁格式检查通过。尚未验证真实采集并发与长时间运行，不据此完成整体目标。

2026-09-17 仓库环图可读性：根据隔离运行中长路径被截断的问题，将显示名称与根路径身份分离；独名只显示仓库名，同名补充最短可区分父目录，悬停显示完整路径。聚合及图形 ID 仍使用原 Git 根路径。新增显示／同名／输入顺序回归检查，280 项测试通过，正常 macOS Debug（未签名）构建与补丁格式检查通过。构建报告先前 QA 输出目录的 stale-file 警告，未手动删除隔离应用或用户数据。此次未重新启动新版应用，标签效果仍待实际界面确认；整体运行及用户验收未完成。

2026-09-17 实际运行进展：使用 `computer-use` 技能发现用户正在运行另一份昨天构建的 Debug 应用，不能作为新工作树验收。新增仅专用 Debug bundle 启用的隔离 QA 模式并补齐余额直接轮询隔离；最终构建完成 100 次 UI 周期切换及一次中途刷新，均选中成功，三个周期与均速独立。独立数据库 quick_check 为 ok、活动／余额均零，原用户进程仍存活。278 项测试与专用 Debug 构建通过。完整边界和未验收项见 [运行证据](macos-runtime-qa-2026-09-17.md)；这不是持续采集、亚秒快速切换、真实历史迁移或声音验收，不将整体目标标为完成。

2026-09-17 文档同步：现行 `PRODUCT_DESIGN.md` 逐章替换旧金额基础，明确机器人各部位、三个周期均有工具模型、授权 Git 成果、静音与显式试听、源新鲜度和完成门槛。`v2-implementation-plan.md` 改为本计划的执行入口，不再保留旧 money-first 完成表；两份旧文本完整移至 archive 供历史追溯。数据文档同步余额净变化／声明月费边界与暂停客户端规则。文档链接目标与补丁格式检查通过；本轮仅文档修改，未新增构建／运行证据。README 仍存在估价、CPL、订阅日摊销和暂停客户端可用承诺，须按 README 技能统一修订；此处不声称文档同步全部完成。

让用户持续感知 AI 使用与消费压力，并看见伴随的产出；不提供伪精确账单、定价追赶或产出价值判断。保留机器人头像和今日／本周／30 天三个独立快照。

2026-09-17 README 同步：使用 `readme-bilingual-visual` 技能，保留浅色矢量主视觉、机器人章节母题、圆角图标与商店按钮及中文引用块。替换旧估价／摊销／CPL／美元 Dock／刷新响铃及暂停客户端已可用承诺；标明 2.0 开发中、截图为历史界面、商店入口不证明发版，删除未核实的通用购买扣费保证。隐私明确区分本地解析、供应商请求和私有 CloudKit 派生摘要／仓库路径同步，不再称任何数据不出设备。主视觉、九张章节横幅和下载按钮渲染并逐张检查，无文字越线／溢出；复用图标贴边及纯矢量约束检查通过，21 个本地资源／文档链接存在，补丁格式检查通过。未推送，未验证 GitHub 最终渲染或新 App Store 文案／截图；本轮未新增构建或 macOS 运行证据。App Store 本地文案与最终实际截图仍须同步。

- 本轮只实现和验收 macOS；暂停 iOS、watchOS、Widget 功能开发与界面改进。
- 共享契约可为 macOS 收口而修改，不为暂停的客户端保留旧模型。记录受影响的消费者，不声称它们已经可用。
- 2.0 不保留 1.0 计费展示、传输和派生模型兼容。CloudKit payload 保持 2.0.0；旧派生缓存可失效重建。
- 不删除用户原始活动、余额、配额或 Git 历史。移除字段与清空历史不是同一件事。
- 不增加 PR／Release 统计；本地产出以 Git 事实为依据。
- 音频素材仍只在本地，不上传仓库；保留来源版权文档。
- 自动测试、构建、实际运行与用户视觉验收分别记录。用户已另行授权完成前整理多个提交或 PR 并推送；未经另行要求不自动合并或发布。

## 阶段 1：数据契约与周期统一

1. 梳理旧金额字段和调用链，删除活动按价表折算、订阅日摊销、预测、每会话成本、每行代码成本及相关失效引擎、设置和测试；核实实际观察金额的独立提醒不会被误删。
2. 分离 `PeriodSummary` 与 `CurrentPulse`：前者包含明确周期、统计和覆盖信息，后者包含当前信号、来源、观察时间及有效期。原币金额保留区间起止和换算来源；未知不写成零。
3. 统一周期起止、时区、桶粒度：今日本地小时，本周周一至今并显示七日槽位，30 天三十日槽位。消除仅由 days 推断语义的云端和缓存回退路径。
4. 保留三个周期独立数据；过期派生缓存重建，切换不互相覆写。周期汇总不能决定当前实时状态。

验收：契约无摊销／估价／预测总消费；无金额来源仍能展示活动；午夜、周一、跨月、夏令时的词元与代码桶一致；三周期交错刷新无污染。

## 阶段 2：统计、归属与新鲜度

1. 独立计算会话数、活跃日与活跃小时；覆盖所有支持的来源，会话身份含来源。
2. 模型工具矩阵对同一单元格求和；不得静默截断（保留全部并横向滚动，若后续缩减展示必须提供“其他”）；缺失模型与未归属活动有说明，分项可与总计核对。
3. 仓库身份使用规范 Git 根路径的稳定 ID，显示名与身份分离；只纳入配置开发目录中的真实仓库。
4. 提交事实独立于代码增减；明确作者／分支／扫描覆盖，分页或可恢复游标避免固定条数漏扫。空提交及二进制变更不因无行数而失去提交事实。
5. 区分工具明确标记、弱时间关联和无归属；一般人类协作者不识别成 AI；当前编辑器不能替历史提交证明 AI 归属。脉搏输出通道遵守同样的仓库范围与归属质量，只作兜底。
6. 配额依据源时间、窗口重置与有效期判断新鲜度；读取旧文件不能刷新源时间。基线不足显示“参考值”，不称“平时”。
7. 提供轻量数据来源状态：可用、过期、读取失败、覆盖有限。采集不可用不等于零活动，历史快照不等于当前状态。

验收：同名仓库／多供应商同名模型／不同工具同名会话不混算；旧配额与范围外代码不驱动脉搏；扫描重试不重复计数；统计含可解释的未知与覆盖边界。

## 阶段 3：详情与全部展示出口收口

1. 详情以会话、词元、模型、仓库、提交和增删行为主，删除估价排序、月预测、CPL 与“是否划算”判断。金额仅在有对应观察事实时独立显示，不分摊给会话。
2. 工具表提供带文字的“查看工具详情”入口，工具行明确可点击；仓库可点项也有明确提示。入口支持键盘、辅助功能和中英文，不依赖悬停才发现。
3. 保留头像与相向分节节奏；无数据绘制分节占位。统一词元、K/M/G/T、一位小数及所有字符串本地化。
4. 仪表盘、菜单栏菜单、主菜单、Dock 右键菜单、Dock 图标和收盘摘要共用语义与格式。历史周期汇总与当前脉搏分开标示。
5. 实际消费事件触发消费反馈；一般刷新仅更新数据，不制造消费动作。当前状态统一自然衰减，过期／未知与休息状态区分；遵守减少动态效果设置。

验收：从主界面能直接发现并打开详情；无隐藏估价；同一事实在各出口一致；刷新不假装消费；词元和代码时间轴对齐且不重叠。

## 阶段 4：声音与运行可靠性

1. 消费单次／连续声音、收盘钟、启动钟分别控制并对应声音包用途；收盘通知不重复播放默认系统声音。
2. 全部自动声音经过统一静音、静音时段及用途开关；消费上限只约束消费提示。明确手动试听策略，避免用户关闭声音后仍被自动提示打扰。
3. 核查系统通知与声音的职责、重复投递、静音跨午夜和启动恢复。审查承诺中的系统专注／全屏策略：明确实现或移除未实现承诺，不将产品静音开关误称为系统专注同步。
4. 实测高速切换、连续采集与刷新、睡眠唤醒、重启、离线／恢复，检查崩溃日志与资源占用。

验收：切换三周期至少 100 次并交错刷新无冻结／串数据；自动声音静音行为一致，一次收盘至多一次提示钟；停止活动后所有当前状态出口回落。

## 阶段 5：文档与 macOS 发布门槛

同步数据事实、产品设计、实施状态及对外描述；多端文档只标记暂停与依赖，不继续实施。记录每阶段变更、测试和剩余验收，旧“全部完成”标记改为历史状态。

最终门槛：

- 单元／契约／聚合／周期／新鲜度／声音测试通过，macOS Debug 与 Release 构建通过；不得把静态 CI 当成运行验收。
- 无旧计费链路进入 macOS 展示或共享有效载荷；真实金额具备来源与观察区间，未知不伪造。
- 真实 macOS 运行验证覆盖上述异常路径，记录哪些是自动验证、哪些需要用户验收。
- 本地音频正常打包且 Git 无素材新增；文档口径与实际行为一致。
- 用户确认详情入口、头像节奏、菜单、声音和日常消费感符合目标后，才开启其他端工作。

## 执行与状态记录

按 1 → 2 → 3 → 4 → 5 推进。每阶段先追踪当前调用链，再实施与回归；不因已有测试通过就跳过新增边界测试。遇到影响用户历史的破坏性迁移或实质范围扩张，先请求确认。

| 阶段 | 状态 | 验证与遗留 |
|---|---|---|
| 1 数据契约 | 实施中 | 已统一本地日历桶与显式周期，修复周一粒度、周视图回退和跨周期缓存；未知金额不生成零汇总。删除旧消费、摊销、预测、牌价参考、工具计费摘要、模型金额、有效价格、工具／仓库金额分配与 CPL。当前脉搏已从历史快照移除，使用独立 CurrentPulseEnvelope 与 CurrentPulse_v2 云记录，默认有效期 60 秒，实时引擎拒绝过期／未来缓存及失效代次。原始活动价格计算、原币观察区间和旧引擎清理仍待完成。 |
| 2 统计与新鲜度 | 实施中 | Claude 缓存以文件修改时间记录观察；配额拒绝过期窗口与未来观察。会话数改为全来源按非空会话身份统计；仓库事实按授权规范根路径聚合，保留仅代码／仅活动仓库，同名不混算；模型单位保留词元和请求。来源内时间戳、提交事实独立记录、弱归属和矩阵截断仍待完成。 |
| 3 展示出口 | 实施中 | 工具详情展示与排序观察词元，摘要独立查询授权仓库相关产出并声明非 AI 创作归属；入口为文字菜单。活跃日按日期去重，矩阵单元格合并多供应商，会话已覆盖全来源。仪表盘实时状态与标签解耦，订阅轻量衰减 tick，未知与有效休息分开说明；输出仅作直接消费信号缺失时的兜底。截断项、范围外输出信号过滤、减少动态效果与实际 UI 验收仍待完成。 |
| 4 声音与运行 | 实施中 | 收盘通知去掉默认系统声音；一般数据刷新不再触发菜单／Dock 消费动画，非空消费批次使用独立事件，静音时段也不抹去视觉活动。新增事件去抖／无假反馈测试，无声测试处理器避免自动播素材。全自动声音策略与真实运行异常路径仍待验收。 |
| 5 文档与最终验收 | 待实施 | — |

2026-09-16 首轮回归：`swift test` 263 项通过；新增中国时区凌晨、夏令时长短日及配额过期／未来时间测试。此结果不代表真实 UI、缓存文件读取链路或最终 macOS 运行验收已完成。

后续周期回归：新增四项显式周期与旧派生快照拒绝测试，267 项通过。暂停的客户端未进行适配，不将其可用性作为本轮完成声明。

旧金额清理回归：删除两项旧混合消费／预测测试，新增禁止八个旧顶层计费字段回归的契约测试；完整 266 项通过。移除约 450 行未使用的 macOS 旧金额／预测图表代码，不修改用户原始数据库历史。共享契约移除的字段将使暂停客户端的旧界面不再兼容；本轮不为其适配。

工具／仓库收口：移除旧工具摘要、CPL、金额分配、有效价格及模型金额；新增活动分项禁止计费字段、仓库身份与聚合测试，删除旧单供应商金额归属与同名合并测试。最终 267 项测试通过，macOS Debug Xcode 构建通过；尚无真实界面与声音验收。

当前状态解耦回归：273 项测试通过，包含过期／未来状态、未知与有效休息区分、独立 JSON 契约、输出兜底、无穷与未来信号以及消费事件去抖。macOS Debug Xcode 构建通过。Debug 仍禁用 CloudKit 写入，未声称独立云记录已实际部署或完成同步验收；暂停客户端不进行适配。

2026-09-17 提交事实收口：新增独立 `git_commit` 与扫描覆盖记录，提交数不再依赖代码增减；包含零行数和合并提交，代码行数仍排除合并。扫描近 30 个本地日历日、当前 HEAD 的可达提交，沿用仓库 Git 作者配置筛选，取消 20 个提交上限；捕获 HEAD 为游标，事实与游标同事务写入，差异读取失败不推进。接入周期图、仓库、详情与菜单，保留仅提交的仓库。历史编辑器邻近归属不再进入输出兜底，仅授权仓库的已识别工具 trailer 可作弱信号，不能视为 AI 创作证据。新增 5 项回归，278 项测试和 macOS Debug 构建通过；未运行新版本写入用户数据库，未完成真实 UI 验收。菜单会话数进一步统一为来源与非空会话 ID 的复合身份。旧 `code_change` 单列提交主键仍需处理跨仓库相同哈希的代码事实遗漏，须保留历史后再变更。

日志采集收口：移除启动金额回填和 `CostBackfill` 实现；日志不再以模型牌价生成或更新金额，也不再向消费反馈发送估价。重复读取保留已有历史金额、不重复报告消费；只有首次插入且时间在最近 5 分钟内的非合成活动才产生词元消费事件，历史／未来记录只更新数据。新增隔离数据库回归覆盖这些边界，279 项测试及 macOS Debug 构建通过。移除未使用的牌价汇总和供应商估价趋势查询。旧日统计／会话内部金额字段、旧 burn 引擎及牌价管理仍待清理，不能将本轮视为金额模型已全部移除；真实 UI 与声音仍待验收。

余额观察独立化：移除 `BurnRateEngine`、旧金额 `AnomalyDetector` 和 `HourlyBaseline`，删除仅用于旧计费行为的 20 项测试；保留并重建余额观察回归，覆盖边界前基准、充值／未来样本、负累计用量、币种切换、未知币种、重复采样时间及非有限值。新增 `BalanceObservation` 仅生成原币净下降与采样起止时间，不负责计费或默认 1:1 换算；日趋势使用同一采样算法并跳过无换算依据的币种。有效载荷携带区间，界面明确非逐笔账单并提供悬停说明，币种不同的同一供应商不再使用重复身份。旧日统计／会话内部金额字段、剩余余额的默认换算以及牌价管理仍待清理。

本轮最终回归：266 项测试全部通过，macOS Debug 构建通过，`git diff --check` 无问题。测试总数降低来自删除旧模型专属测试，不是减少新模型验收范围；尚未进行真实 UI、CloudKit 或发布版运行验收。

会话模型清理：移除 `SessionRow.cost`、`RepoSessionGroup.totalCost`、`TurnPoint.cost`、上下文金额汇总、月末预测、金额同比与缓存节省金额函数；详情查询不再读取历史金额，仓库／会话按观察词元排序并确定性处理相等值，保留来源＋会话 ID 及完整仓库路径身份。详情周期上界使用日历加一天，排除空会话及合成活动。删除 5 项旧金额函数专属测试，新增会话复合身份／同名仓库测试，既有排序测试改为验证词元。

余额币种收口：剩余余额快照与配置行保留原币，不再默默按固定汇率展示美元；剩余余额读取拒绝未来与非有限值。移除默认未知币种 1:1 的 `toUSD`，金额阈值提醒使用显式换算，并跳过未知币种、跨币种和非有限净变化。静态换算仍为估计，不视为服务商实际账单。

新增必须修复的词元事实问题：当前 Claude 解析器将 `input_tokens` 和 `cache_read_input_tokens` 分开保存，现有测试允许缓存大于输入（100／200）；这与 `TokenAccounting` 的“所有缓存均为输入子集”注释及总量 SQL 不符。上下文详情暂保留 Claude 输入＋缓存的实际口径；下一轮须按真实解析格式统一全链路词元统计，并检查缓存创建字段，而不是根据旧注释改写历史。旧日统计／演示数据金额和牌价管理仍未清完，不能声称数据模型已自洽。

会话／余额本轮最终验证：262 项测试通过，macOS Debug Xcode 构建通过，补丁格式检查通过。此结果不证明原币 UI 已获用户确认或上述 Claude 词元问题已解决；真实运行、最终发布门槛继续保留。

Claude 词元事实修正：按原始解析格式保留未缓存输入、缓存读取与新增可空缓存创建字段，统一 SQL／Swift 边界计算；Claude 总输入为这三个观察分项之和，Codex／Qwen 不重复加缓存。详情上下文、总量、菜单、当前脉搏和日志消费事件接入同一边界，并修复详情 SQL 的来源别名歧义。缺失缓存创建的近期 Claude 活动标为部分观察；缺失值不当成已确认的零。仅重置用户 `.claude/projects` 下 JSONL 的派生读取位置以回读可用原始日志，回读按去重键补充缓存创建元数据、不重写旧输入／金额、不触发新消费。原始日志不可用时仍保留缺失状态。新增 5 项测试覆盖分项、Swift／SQL 一致、子集、不溢出和窄范围回读。其他解析器（尤其 OpenCode 与 DeepSeek Harness）的分项语义仍须进一步核对，不能声称所有来源已统一验证。

依据：[Claude 官方提示缓存文档](https://platform.claude.com/docs/en/build-with-claude/prompt-caching) 与 [Claude Code 状态栏文档](https://code.claude.com/docs/en/statusline) 确认总输入含 input、cache read、cache creation。旧“所有来源缓存均为输入子集”说明不再成立，权威数据文档仍须同步修订。

Claude 本轮最终验证：267 项测试通过，macOS Debug 构建通过，补丁格式检查通过。只验证了隔离数据库中的回读选择、去重及元数据补充，尚未启动新版本对用户完整日志执行回读或确认实际界面变化；其他来源语义和最终 macOS 运行验收继续保留。

DSH／OpenCode 分项收口：DSH 官方 TokenUsage 定义与本机 `@deepseek-ai/dsh-llm` 0.1.5-rc.2 均确认输入、缓存读取、写入互不重叠；推理已包含于输出。OpenCode `Session.getUsage` 保存的是互不重叠输入、缓存分项及分开的输出／推理。修改解析器并接入统一归一计算，新增可空的报告输出和推理字段；已有旧输出与金额不重写，去重回读只补元数据。DSH 缺失报告输出时不使用旧派生输出，近期分项缺失标为部分观察；重新读取可用 DSH 日志，OpenCode 原有逐文件扫描会补充可用元数据。保留缓存活动但输入／输出为零的 OpenCode 消息。新增两项端到端解析→数据库→SQL／Swift 回归，269 项测试与 macOS Debug 构建通过。权威数据文档已修正词元章节和历史审计适用范围；完整文档同步仍未完成。

依据：[DSH 官方记账定义](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/llm-streaming.zh.md)、[OpenCode 分项实现](https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/session/session.ts)。下一步须核对 Codex 输出／推理关系，补所选周期完整性说明并继续清理日统计／演示金额；当前不能将所有工具计量或运行验收标为完成。

Codex 输出及周期完整性：核对 [Codex 上游 SSE 转换](https://github.com/openai/codex/blob/main/codex-rs/codex-api/src/sse/responses.rs) 与协议，修正解析器输出重复加入推理的问题。新解析保存报告输出与推理细项；旧派生输出不改写，没有报告分项时不作为可信观察，回读仅重置本用户 sessions 下 rollout JSONL 的派生位置。SQL／Swift 与消费事件使用相同口径。新增 `ActivityCoverage` 保存所选周期的已观察行数和缺失分项行数；查询失败使用未知计数，不伪造零活动。界面显示部分观察／完整性未知，并说明支持的本地日志不等于账户全量或账单。无覆盖字段的旧派生快照失效重建，不新增旧展示兼容。补五项原始输出、历史保留、窄范围回读、覆盖状态和契约测试。真实日志回读与实际 UI 验收仍未执行，旧日统计／演示金额链路仍待清理。

本轮最终回归：274 项测试通过，macOS Debug 构建通过，补丁格式检查通过。日活动 `TrendPoint.value` 改为词元，不再通过此通用字段传输历史估价；本地 `DailyStat` 内部金额与演示派生金额仍待删除。观察完整性说明尚未获真实界面验收，统计读取失败／未来数据／矩阵截断和仓库代码事实主键仍需进一步收口。目标仍在实施中，不开展暂停客户端适配。

日统计／演示事实清理：删除 `DailyStat.cost`、每行成本与 `ProviderDailyCost`，日／小时活动和模型统计不再读取历史 `cost_usd`；保留原始历史字段和独立余额观察。演示模式不再分配虚构供应商、工具或仓库金额，按相同本地小时事实汇总所选周期；顶部、工具模型、仓库和节奏图总量一致，提交数独立于代码行数，并消除静态启动日期造成的跨午夜错误。菜单使用相同事实源。新增三项回归覆盖三周期分项一致性、无虚构金额、午夜和夏令时边界。277 项测试通过、macOS Debug 构建通过、补丁格式检查通过；未启动新构建写入用户数据，未进行真实界面／声音／CloudKit 验收。旧牌价管理、统计覆盖与矩阵截断、跨仓库代码事实主键及最终运行验收仍待处理，目标保持进行中。

工具模型矩阵收口：真实查询保留空／缺失模型，使用与周期一致的半开边界；抽取可在隔离数据库直接验证的查询。矩阵以供应商与模型为复合身份，同单元格累加，未归属工具单列显示；消除前 6 工具／8 模型的静默截断，保留全量并允许横向滚动，因此各单元格与行列总计具有相同范围。新增三项回归覆盖查询边界／缺失模型、重复单元格与同名模型、多轴全量总计及稳定排序。280 项测试、macOS Debug 构建、字符串 JSON 和补丁格式检查通过；横向滚动的实际布局和用户可读性仍待验收，不据此宣布所有统计或 macOS 目标完成。仓库词元查询的结束边界、查询失败覆盖及未来数据规则仍需统一。

仓库词元周期边界：修正仓库词元查询仅有起点且纳入合成记录的问题，使用同周期半开区间和与模型／工具一致的合成排除条件；保持调用方规范化与开发目录授权，不把工作区路径直接变成仓库。新增隔离数据库回归验证起点、终点、缺失模型及合成记录，核对全部记录已归属时仓库与模型总量一致。281 项测试、macOS Debug 构建与补丁格式检查通过。旧牌价估算 API 和未调用的金额来源仲裁仍需删除；真实运行、查询失败覆盖与未来数据规则仍未验收，目标继续进行。

牌价及仲裁移除：删除已无生产调用的 `Arbitrator` 与六项专属测试、词元折算金额 API 和七项旧计费测试；牌价目录替换为仅含名称／供应商的模型识别目录，更新所有 macOS 调用及 Xcode 资源引用。移除估价／摊销可信度与未调用的界面徽标；订阅配置使用声明状态，无余额接口标为观察不完整。保留用户原始历史，不将模型识别视为账户付费证据；修正相关采集注释。新增目录字段与旧状态拒绝测试，捕获并删除残留定价备注。最终 269 项测试通过、macOS Debug 构建及补丁格式检查通过，目录资源已随构建替换。该轮未进行真实应用运行、CloudKit 或声音验收，配置结构命名、代码事实主键、统计覆盖及最终运行验收仍需收口。

代码事实仓库身份：新代码表以仓库路径＋提交哈希唯一，不再跨仓库排斥相同哈希。旧单哈希表在写事务内改名为 `code_change_legacy_raw` 原样归档，原 ID、时间、行数和归属完整复制到新表；归档不参与统计，也不是旧展示兼容。仅重置派生 Git 扫描 HEAD，已有提交事实与覆盖起点保留，下一次在原扫描范围内补读可用仓库；无法读取的历史不能声称已恢复。迁移重复调用为无操作，意外同名归档冲突报错，不覆盖原表。三项新增回归覆盖跨仓库事实与去重、历史字段保留／游标重置及冲突失败原数据不变。272 项测试、macOS Debug 构建和补丁格式检查通过；未对用户数据库执行迁移，实际补扫、UI、声音和 CloudKit 仍待验收。

Claude 配额源时间：核对本机状态缓存结构，其根字段 `updatedAt` 是带毫秒 ISO 时间。配额读取采用该源时间与文件时间中较早者，不以当前读取时刻或复制后的文件时间续期；源时间缺失、无效或未来时间不构成新观察。移除历史模型 DISTINCT 去重后排序的 eligibility 查询，改为最近五小时最新非合成、非未来、非空模型；近期非 Claude 模型不再被旧 Claude 模型覆盖。模型家族只是适用性过滤，不证明订阅付款。配额持久化拒绝非有限值／无效窗口。新增两项回归覆盖触碰／复制、未知／未来源时间、近期第三方模型及未来／过旧活动。274 项完整测试、macOS Debug 构建与补丁格式检查通过；未刷新用户配额表或完成真实运行，新鲜度界面与覆盖状态仍需验收。

声音静音边界：自动播放入口统一重查全局静音、静音时段、用途和非零有限音量；双次提示的延迟第二声重新检查当前设置与取消代次。一键静音回调停止当前 AVAudioPlayer 并取消待发第二声。素材缺失、播放器拒绝或解码失败不再改用系统蜂鸣，避免绕过独立音量。显式试听独立标记，可绕过自动用途／静音时段，但不绕过全局静音和零音量。删除设计文档中未实现的系统专注／全屏自动静默承诺，系统通知由 macOS 设置控制，不等于应用自播音效同步。新增两项纯策略回归，276 项完整测试、macOS Debug 构建和补丁格式检查通过；本轮未真实播放音效，停止正在播放的效果和延迟取消仍需实际运行／听感验收。

收盘摘要出口：删除旧版本最终句子的写入和兼容键，使用独立 v2 结构化事实缓存，在菜单读取时按当前语言生成。成果不再汇总全库带归属标签的行数，改用授权 Git 根中的非合并代码变化及独立提交事实，含仅提交产出；词元和代码采样截止于本次收盘时刻，配额拒绝未来／非有限样本。失败的词元／代码读取保留可空事实，不编码成零；金额保留原币并统一一位小数，代码与提交使用紧凑单位，不称 AI 创作。慢查询结束后在主执行域重新核对关闭开关与当日已发标记，防止重叠任务重复收盘。新增两项摘要回归覆盖仅提交与未知事实，既有本地化／原币／结构化回归同步修订。278 项完整测试、macOS Debug 构建、字符串 JSON 与补丁格式检查通过；真实收盘通知／钟声及并发任务运行仍待验收。
