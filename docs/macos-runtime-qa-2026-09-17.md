# macOS 2.0 隔离运行检查

> 2026-09-17 · 部分运行证据，不是完整 macOS 验收。

## 十语言实际选择控件与中文／英文工具可见性

当前专用QA通过既有应用语言选择器依次选择English、简体中文、繁體中文（台灣）、繁體中文（香港）、日本語、한국어、Deutsch、Français、Español、Português (Brasil)。每次动作后重新读取AX：选择器正常显示目标语言；英文设置的General和独立Notifications入口更新；所检查的settings.general／general.group_notifications／health.impaired键未原样显示。共十种语言，选择失败0，三条关键键泄漏0。这个检查不证明全部字符串、仪表盘排版、曲线或新版故障提示的实际绘制通过。

未切换任何集成开关：英文开发工具页Codex及Claude可见；简体中文Codex不可见，Claude仍可见。源码受限ID列表含openai／anthropic／codex，不含claude-code，因此本地Claude Code卡片可见不能当作Anthropic账户卡片的可见性结论；本轮仅核对现行规则，不扩大地区限制。恢复原有“跟随系统”应用语言并确认选择器状态后退出QA，正常应用保留。无试听、通知、采集启动、系统语言更改或新目录授权；完整视觉／语言覆盖仍待验收。

## 采集故障健康提示的十语言文案修正

实际故障界面中，采集写入失败却显示“部分查询失败”的泛化健康提示。health.impaired十语言改为读取或采集失败，仪表盘及共用菜单保留同一键；不改变故障账本或恢复条件。JSON结构、十种非空translated条目及补丁格式检查通过，正常macOS Debug／Release及专用QA Debug顺序构建退出码0。本轮只改资源文案，生产Swift逻辑未变；最近对应逻辑的341项测试已通过，本轮未重复运行，不称新版文案已实际渲染验收。未启动应用、试听、改变系统设置或重试未获准的目录授权。

## 当前生产采集器实际写入失败与检查点保留

源码503344f的专用QA使用已在线备份的隔离库：临时BEFORE INSERT触发器拒绝活动写入，经现有设置显式启动生产LogWatcher／phase1。实际退出演示后，界面显示“部分本地日志读取或写入失败；空图不代表没有活动”，以及不完整健康提示。活动数量未变，与备份全部活动字段双向EXCEPT均0，持久检查点与备份差0，quick_check为ok；实际写入失败／失败提示／检查点不提前推进子项通过。本次公开补充不记录私人路径、进程标识或活动数量。

立即移除拒绝触发器并核对其数量0。通过现有目录入口计划原地重试，目录授权被安全审查拦截；取消选择框，不重试该授权。此时活动数量未变、历史核心事实无缺失／重复0、检查点未推进，不能声称采集已恢复。退出后确认QA结束、正常应用保留；无试听／通知／系统设置修改，正常库及原日志不改动，备份仅本地保留。故障注入已解除，不把应用退出清空内存故障账本称作成功恢复。

## 日志增长后失败恢复键的修复

源代码检查发现写入失败使用批次数量及首尾去重键登记；日志继续增长会改变重试批次，成功可能留下旧故障。移除批次健康键，JSONL／DSH在整份文件读取完成后统一清除稳定Log.file路径键；OpenCode单文件写入同样报告／恢复，位置保存故障仍独立。新增生产结果登记入口回归确认一个文件恢复不清另一个文件故障。10项定向与真实本地回归显式启用的341项全量测试通过；正常macOS Debug、Release及专用QA Debug顺序构建命令退出码0，九音频包一致性／Git忽略、新模型目录一致性及旧价格目录缺席检查通过。构建期间旧QA产物路径警告不是构建失败，未据此删除任何产物。

本轮尚未启动新QA、注入活动写入故障或播放声音。此为已修复且自动验证的恢复逻辑缺陷，不宣称真实故障界面／连续恢复已通过；下一次运行须使用最新专用QA产物。

## 单实例边界及生产日志采集运行窗口的切换／刷新

用户提醒单实例限制后核对生产判定：NSRunningApplication只筛选当前bundle ID；原Xcode测试产物为xingyu.wang.aipulse.debug，专用QA为xingyu.wang.aipulse.runtimeqa，不触发相同bundle单实例退出。QA启动演示来自其显式注册默认值及无配置来源的自动演示逻辑，已在界面实际退出；不把正常bundle的同ID重复启动与此隔离模式混为一谈。原实例无需为此次不同bundle测试关闭；若后续运行同ID新产物，按用户授权先关闭旧实例。

当前源码QA PID64404，先在线备份专用库到本地临时目录，通过现有添加目录入口仅在QA增加项目AIPulse子目录，生产LogWatcher.start及phase1显式运行。活动81,876→82,233（初次观察）→82,235；备份的原活动ts／source／输入输出／缓存／原金额／去重键组合在当前库无缺失，重复0、检查点597、数据库检查ok。设置显示8目录／1仓库；无祖先仓库向上认领。

首次回仪表盘时已显示最近扫描完成，因此不称扫描中切换。首20次选择全部通过，其后观察又新增2条；第二组20次选择及4次刷新后活动仍82,235、重复0。累计40次各次重新读取AX核对选中周期与无演示残留，失败0；不是亚秒测试，不证明每次切换／刷新都有同时在途写入。最终缓存today／week／30d独立存在、提交仍64、quick_check为ok。一次缓存检查误用period_key列失败，读取实际schema后以time_range复查；不把失败查询当一致性证据。

RSS抽样422,560KiB（采集中CPU96%）、383,968KiB及186,544KiB（后两次CPU0%），不是全程峰值或长期证据。最终仍显示最近扫描完成的有限覆盖提示，无演示；退出后ps确认QA结束，正常旧PID84482保留。无试听、系统通知、账户轮询或系统设置修改，原日志仅本机读取，备份不上传。运行窗口采集新增／事实保留／切换刷新子项通过；采集实际失败后的连续恢复、严格并发压力及系统生命周期仍开放。

## 磁盘SQLite写入故障／重新打开／恢复的组件回归

新增JSONLCheckpointTests使用独立临时JSONL和磁盘SQLite，生产完整行读取器、事件持久化及LogCheckpointStore。SQLite触发器拒绝ts大于512的插入，第二批真实数据库写入失败；首批512条已提交，内存及磁盘游标仍为0。关闭并重新打开数据库后移除拒绝触发器，从持久游标重读，最终600条／600个不同去重键，持久游标到完整文件末尾、quick_check为ok，历史补录产生的实时消费词元为0。临时数据由测试清理，不涉及用户库或日志，不调用播放器。

首轮新测试编译因累计值误用Int失败，改为生产接口Int64后定向4项通过；显式启用全部既有真实本地回归的全量340项通过。本轮只新增测试与记录，生产源码仍为17b4ad7，沿用其已完成的正常Debug／Release及专用QA构建。此证据补齐组件级磁盘失败和重新打开恢复，不宣称完整LogWatcher持续运行、源故障UI或系统重启验收通过。

## 系统菜单栏／Dock读取超时，不视为应用冻结

当前构建QA PID62644的应用菜单栏暴露文件／AI Pulse／窗口。系统Dock和SystemUIServer界面观察各一次超时；随后同一QA仪表盘、今日／本周／30天选择控件及刷新按钮仍可读取。没有操作系统设置或改动系统进程，不称系统菜单栏／Dock通过。此轮不重复此前失败路径，视觉及右键导航转人工验收。退出QA后ps确认PID62644已结束、正常旧PID84482保持运行。没有音效播放、新日志采集或实际睡眠操作。

## 授权后最新生命周期构建的真实主菜单回归

用户确认本机隔离界面读取及验收文档远端公开范围后，源码17b4ad7和文档318427c分组推送到codex/macos-v2-closure；git ls-remote核对远端318427c373e762c8e1368b90eebf91e73674c234。没有创建PR或发布。

最新专用QA构建启动PID62329；首次观察超时后确认同一进程仍在，未重复启动。完整AX输出因可能含会话正文再次拦截，采用安全审查建议的控件过滤方式，不输出会话标题／正文。实际退出演示后，今日231.5M／6次与已有快照一致；主菜单今日231.5M、+3.3K/-7.7K行、6次，本周506.0M、+8.4K/-20.7K行、8次，仓库项8次及一键静音入口存在。实际点击本周菜单后单选状态为本周、506.0M／8次，无演示残留。没有开启采集或新Git轮询，因此这些是已有真实快照一致性，不声称跟上当前HEAD。

退出QA后确认PID62329已结束、正常旧PID84482保持运行，无试听、通知或系统设置修改。生命周期补丁的当前主菜单呈现／本周导航子项通过；菜单栏、Dock、轨迹曲线视觉及真实睡眠／长时间运行等仍未验收。

## 最新专用 QA 构建及界面读取权限边界

生命周期补丁的专用隔离 Debug 构建通过。QA 启动 PID 59756，首次观察超时后确认该进程仍在，未再次启动；完整界面树读取被安全审查拦截，原因是可能含真实会话标题／正文，因此没有新增菜单或界面运行结论。未重试读取；GUI退出操作因未成功取得界面状态而无法执行，改向已核实的独立QA PID发送TERM，正常旧 PID 84482不在目标范围。无试听、通知或系统设置修改。后续真实数据界面检查及验收记录公开推送分别等待访问范围／公开范围确认。

## 生命周期幂等补丁的最新构建门槛

协调器重复 start 不再覆盖定时器／观察者，stop 清除暂停状态；未启动调度时接收数据通知的原有行为保留。新增进程内模拟通知的状态回归，定向9项和全部真实本地回归显式启用的全量339项均通过。首轮候选因影响通知能力失败，修正后复测通过；不隐藏首轮结果，也不将模拟通知称作系统睡眠验收。

最新未提交源码依次完成正常 macOS arm64 Debug／Release 构建，整体命令退出码0；静默期间确认同一 xcodebuild／编译进程仍在运行，未重复启动。九个 MP3 与当前 Release 包逐个一致且全部被 Git 忽略，声音目录只跟踪来源文档；新模型目录与包一致、旧价格目录不存在、执行文件为 arm64。未启动正常应用或播放音效。专用 QA 构建／运行尚需更新，系统睡眠与长期资源验收仍开放。文档远端推送等待用户确认公开范围，不重试安全拦截。

## 真实会话加载与活动查询失败／恢复

当前源码 f8fa035 的隔离 QA PID 58470 退出演示后，经明确入口打开真实 Codex 详情、展开仓库与会话。界面显示完整会话截至本次读取、曲线仅含有效输入观察、部分字段缺失及输出统计；轨迹为 2,546 个回合，没有读取失败提示，底层仪表盘不在详情的可访问性树中。Escape 返回仪表盘成功。仅验证加载、范围说明和返回；没有通过可访问性树验证曲线绘制，不称完整视觉或键盘验收。会话正文与标题仅留本地，未写入本记录或上传。

对专用 QA 库先做 SQLite 在线备份，再临时将 usage_event 改名、清除派生 dashboard_cache，点击实际“刷新数据”按钮。真实界面出现读取失败及未知占位，而非健康零；独立 Git 事实仍显示今日 6 次提交。立即恢复表名、清除派生缓存并再次刷新：读取失败消失，今日 231.5M 词元和 6 次提交恢复，无演示残留。恢复后活动表全部字段与备份双向 EXCEPT 均为 0，活动数 81,876，quick_check 为 ok。备份仅保留本地。这是活动查询失败／界面恢复证据，不是日志采集写入失败、采集连续恢复或实际断网验收。

未修改正常用户库、系统设置、通知权限或音频，未试听。退出 QA 后 ps 确认 PID 58470 已结束，正常旧 PID 84482 保持运行。本轮没有源码修改，沿用当前源码已完成的 338 项测试及 Debug／Release 构建，不重复构建文档变更。

## 当前刷新入口与100次真实周期选择／5次刷新交错

刷新原仅为底部文字 onTapGesture，AX 无独立按钮。新增明确“刷新数据”按钮（十种语言、刷新时禁用），保留版本与更新时间；手动刷新先失效缓存并取消各周期旧请求，再捕获各自代次，新快照只在对应代次仍有效时发布，结尾不撤销期间产生的新请求。移除未使用的按已有周天数变量。源码提交 f8fa035；显式真实副本／Codex／DSH／Git 根的338项全量测试及正常 Debug／Release 构建均退出码0，专用 Debug QA 构建通过。九个 MP3 与最新 Release 包逐个一致且 ignore、新模型目录一致、旧价格目录不存在、arm64 执行文件检查通过。

当前构建隔离 QA PID57807 退出演示后，AX 明确暴露按钮“刷新数据”。真实三个周期按30天／本周／今日循环100次，每20次发起一次刷新，然后相邻发送周期选择请求；每次重新读取 AX，选择状态、无演示残留、提交数6／8／64全部匹配，累计5次刷新、失败0。此为相邻请求与界面观察，不称亚秒测试，也不能证明每次都存在同时在途的查询；期间无持续日志采集或新Git提交，不能替代这类压力门槛。

结束后活动数量仍81,876、提交64，持久缓存独立为today／week／30d，quick_check为ok。资源单次采样：50次后RSS412,832KiB，完成后167,408KiB（约164MiB），CPU均0%；不是全程峰值或长期运行证据。真实顶部菜单今日231.5M／6次、本周506.0M／8次及代码变化与仪表盘一致。取消菜单后退出QA，ps确认已退出、正常旧PID84482保留。未试听、投递通知、改变系统设置或修改正常用户库。全部菜单／Dock、完整轨迹、实际故障及系统生命周期验收仍待完成。

## 14:38—14:43 新提交增量与周期缓存真实回归

使用本目标已产生的实际提交，不制造测试提交。采集前独立提交为 57、活动为 81,876；当前源码首次 QA PID 56826 一次性生产 Git 轮询后提交为 63／complete，本地 Git 同作者／coverage_since 计数也是 63，提交哈希集合双向差为 0、游标等于实际 HEAD、范围外及重复为 0、quick_check 为 ok，活动数量不变。今日 5 与 SQL 一致，但实际切换本周／30 天仍显示旧 1／57，再次读取仍不变：提交入库成功不能证明派生界面已更新。

确认两个原因：Git 变更通知未失效持久周期缓存；仪表盘数据变化只重载当前周期。修复为等待 Git 缓存失效后再通知、数据变化重载三个独立通道（由协调器统一去抖，取消仅作用于对应周期）；新增无声回归确认失效先于通知且不制造消费事件。338 项完整测试（真实副本／Codex／DSH／Git 根显式启用）、当前专用 Debug QA 构建及补丁格式检查通过。源码提交 c6c9cf2，正常 Debug／Release 尚需在此补充源码后更新，不能引用上一轮 337 项源码构建作为最新证明。

退出首次 QA，保留同一真实数据库，修复构建 PID 57368 再次显式生产增量轮询捕获修复提交：提交 64／complete，活动仍 81,876。实际今日 6、本周 8、30 天 64，头像及仓库表与 SQL 一致，旧周期值未再保留；本地 Git 哈希集合差 0，扫描游标等于新 HEAD，重复／范围外 0，quick_check 为 ok。未播放音效、开启通知或账户轮询；退出 QA 后 ps 确认已结束，正常用户旧 PID 84482 保持运行。新提交增量、游标推进及三个周期更新子门槛通过；不是在不停机应用中交错新提交和百次切换的压力验收，实际故障恢复／睡眠仍待完成。

## 14:35 收盘发送前边界，无声自动回归

最终构建记录：正常 macOS Debug 后依次运行 Release，命令均退出码 0；Release 静默期间通过 ps 确认具体进程仍在运行，未重复启动。对应源码补充提交 0082512，全量 337 项测试通过。此次未启动应用、试听或投递通知，真实行为验收仍保持上述边界。

发现慢查询返回后只重查开关及日去重标记，未重查当前日和最新收盘时间。发送前统一调用主 actor 的 ClosingBell.claimDailyDelivery：捕获的日开始必须仍为当前日开始、提示仍启用且最新配置时间已到，随后无 await 地认领日标记。64 个并发认领只有一个成功；跨日、关闭提示及把时间推迟都不认领标记，恢复有效设置后可以认领。两项新增回归及显式启用真实副本／Codex／DSH／Git 根的全量 337 项通过。所有新测试均不调用 fire 或播放器，不播放音效；不把认领回归当实际通知投递或真实系统睡眠验收。正常 Debug／Release 构建完成状态另行记录。

## 用户确认音效，取消重复试听

用户明确表示音效都已确认没有问题，不需要试听。准备验证时隔离 QA PID 55922 已启动，界面仅打开通用设置；收到要求后未点击通知页面或任何试听／播放按钮，直接退出。ps 确认 QA 已结束，正常旧 PID 84482 保持运行，系统音量、通知权限和音频素材未修改。音效素材与听感以用户确认为证据，不冒充助手实际播放；自动触发、立即停声、静音时段、限次和收盘去重仍独立核对，后续不再主动试听。

首轮提交对照：源码及回归测试 df7babd，产品及权威数据文档 4cad3e2，首轮运行记录和收口计划 8e79e8e；三个提交已推送并核对远端 SHA。本轮收盘补充源码为 0082512，用户音效确认及新验证记录另行提交。分支 codex/macos-v2-closure 基于已合并 PR #68 的最新 origin/main；旧分支保留。以下历史各轮的未提交／未推送状态是当时记录，不代表当前提交状态。仍未验证的运行项保持开放，不因整理历史或推送而视为已验收。

## 当前源码正常 Debug／Release 与打包门槛更新

本轮不改源码，依次完成正常 macOS／arm64 Debug 和 Release 未签名构建，两者退出码 0。源码已含根目录扫描、系统通知统一静音时段和仅隔离 QA 的一次性 Git 探针。Release 目标目录通过 xcodebuild showBuildSettings 核对为 DerivedData/AIPulse-egrmrgljavzerbaxipffvtjdqugc/Build/Products/Release，产品 AI Pulse.app。

九个 MP3 与包中 Sounds/{coin,droplet,register}/{coin,coins,chime}.mp3 逐个 cmp 一致，逐个 git check-ignore 成功；声音目录 Git 仅跟踪 README。model-catalog.json 与包中一致，旧 pricing-catalog.json 不存在；新目录无 price／pricing／cost 键。Info.plist 的 CFBundleExecutable 为 AI Pulse，按完整带空格路径读取 file，确认为 Mach-O arm64。首次使用 xargs 检查执行文件时因路径空格拆分未成功，已改用精确引用路径重查，不把该首次调用视为证据。

最近全量 335 项测试覆盖相同源码，本轮没有重复测试或启动正常应用。实际听感、系统通知投递、全部菜单／Dock、睡眠唤醒及用户消费感仍未完成。用户新增授权：在目标完成前将工作分为多个提交或多个 PR 推送远端，不自动合并或发布；此轮尚未提交、推送或创建 PR。

## 14:24—14:26 真实 Git 轮询、周期展示与重启去重

新增仅专用 Debug QA bundle 可消费的一次性偏好 qa_poll_local_git_once；启动消费后立即移除，再调用生产 GitMonitor.poll，不开启采集定时器、账户轮询、云写入或通知权限。正常 bundle 即便存在同名偏好也不执行探针。通过 CLI 仅设置 QA 偏好域，computer-use 启动 QA PID 53983；观察启动超时后确认进程已存在，再读取同一 bundle，未重复启动。

生产轮询读取本项目真实 Git，独立 git_commit 写入 57，git_commit_scan 为 1／complete，范围外提交 0，仓库＋提交哈希重复 0，quick_check 为 ok。采用入库扫描 coverage_since 和本项目配置作者邮箱，用只读 git log 独立计数为 57；不输出邮箱、提交消息或用户日志。退出演示后，真实仪表盘今日 0、本周 1、30 天 57，与对应数据库时间边界计数一致；30 天和本周均显示实际代码变化，今日没有已提交变化，因此为 0，而不是今天的未提交修改自动计入。

退出 QA 后再次显式设置一次性探针并重启 PID 54026；第二轮扫描 updated_at 距只读观察约 26 秒、状态 complete，提交仍为 57，重复仍为 0，quick_check 为 ok。最后退出 QA，ps 确认两个 QA PID 都已结束，正常旧 PID 84482 保持运行。仓库发现／真实提交入库／三个周期提交展示／同 HEAD 重启轮询去重子门槛通过；尚未验证新提交到达、故障后的连续恢复或真实系统睡眠唤醒，不关闭整包 2／5。

另修正已确认的静音遗漏：余额消费及健康系统通知原先只判断 sound_muted，现在共用 AppSoundControl.permitsNotificationSound，同时遵守静音时段；文字通知仍由系统通知设置独立控制，不受消费音包开关或其次数限制影响。新增跨午夜、首尾边界、关闭静音时段和全局静音回归；显式启用真实副本／Codex／DSH／仓库回归的全量 335 项通过，当前专用 QA Debug 构建成功。未开启或实际投递系统通知，不把策略测试当实际声音验收。最终正常 Debug／Release 构建仍需更新。

## 14:20—14:23 仓库计数真实界面复测

当前源码专用 QA Debug 构建成功，QA PID 53414 启动；首次界面观察超时后确认进程已存在，再读取同一应用，未重复启动。退出自动演示后，仓库设置真实显示本项目根 1、docs 0、Sources 0，总计 6 个目录／1 个仓库；此前根目录为 0 的界面问题已复测通过，子目录未向上认领祖先仓库。

通过已有添加目录入口仅为 QA 添加本项目 Tests（显示 0），显式触发生产 phase 1。提交表及提交扫描表仍为 0；只读查询确认历史 gitmonitor_state 已包含本项目（1 行）。代码证据：RepoDiscovery 只注册未知项目，GitMonitor.watch 只在新注册时扫描；QA 禁用自动 coordinator.start，故恢复监控名单中的本项目没有触发 phase 2 轮询。这个操作不能证明独立提交入库，也不能把零提交直接判为生产采集失败；下一步须验证实际 Git 轮询，而非继续添加无关目录或把合成测试当运行验收。

QA 数据库 quick_check 为 ok。通过应用退出命令结束 QA，ps 确认 PID 53414 已退出；正常用户旧 PID 84482 保留运行。正常数据库／系统设置／通知权限未改，未提交或推送。仓库计数子门槛通过，提交采集及整包 2 仍待验收。

## 14:19 仓库根目录扫描修复与回归

真实 QA 中直接配置本项目根目录仍显示零仓库，代码追踪确认 GitRepoScanner 的 FileManager.enumerator 只返回子项，未检查根本身。设置 RepoScanCache 与生产 RepoDiscovery 共用此扫描器；显式 triggerIngest 会运行生产发现阶段，QA 禁用周期调度并不禁止这个手动入口。发现后 GitMonitor.watch 仍验证真实 Git 根及配置范围，并安排现有提交扫描。因此根遗漏会阻止此路径注册项目；零提交的运行问题仍须在修复构建中验证，不能只归因于 QA 禁用了定时器。

修复保留现有候选标记、浅层优先、worktree 文件及期限语义，只补根边界，不向上扫描普通子目录。五项合成回归验证根、worktree、期限、子目录边界及缓存持久化；另一个显式 AIPULSE_QA_REPO_ROOT 只读检查确认本项目 libgit2 根与扫描唯一候选一致。全量 334 项测试通过，同时显式启用此前真实副本迁移、Codex 和 DSH 日志测试；正常 macOS Debug／arm64 未签名构建退出码 0。Xcode 有既有 QA 产物位置警告，不是构建失败。此轮未启动应用或改变正常数据库，界面计数与提交实际入库尚未复测。

## 14:05—14:14 内存修复的完整真实扫描复测

先确认 QA 已退出并保留上轮采集库至 qa-before-memory-replay.db，再恢复同一 68,435 条真实基准，quick_check 为 ok；不改正常用户库。重建当前源码专用 Debug QA，通过现有目录设置添加本项目 docs 子目录显式启动全扫描，随后添加 Sources 子目录再触发增量扫描；两者仅 QA 配置，既有项目根授权仍保留。账户轮询、Debug 云端写入及系统通知仍禁用，全局静音保持。

实际扫描中 → 最近扫描完成 → 两分钟后观察过期 → 第二次扫描后最近完成，均从 AX 树读取，不用模型或单项测试替代运行证据。首轮扫描后 Codex 54,338／DSH 17,628，460 个非零检查点，quick_check 为 ok、source＋dedupe_key 重复零。第二次实际增量扫描新增 18 条 Codex，最新源时间 14:13:01，DSH 未增加，重复仍零；不是重放计数增长的证明，因为这段时间真实日志也新增。与基准只读 EXCEPT 对照，原 68,435 条活动的 ID、时间、来源、输入／输出／缓存计数、历史金额、会话、去重键及金额归属全部仍在且未改写；供应商／模型／路径允许事实补全，不纳入此不变性声明。

同一当前修复构建完成 100 次真实三周期切换，逐次用最新 AX 索引操作，检查对应 Value 1 且无演示标记，失败零；开始阶段与实际采集／数据变化交错。它满足本轮实际 100 次选择检查，但自动等待的操作不称亚秒压力，也没有执行专门的手动刷新交错。本周七天／30 天固定周期与今日小时均速仍独立。

QA PID 52094 的 ps 单点 RSS（KiB）：运行 01:32 扫描中 633,840（约 619 MiB）、03:24 完成后 515,472（约 503 MiB）、04:48 为 330,352（约 323 MiB）、07:35 为 295,024（约 288 MiB）、第二次增量扫描后 08:18 为 409,280、08:39 为 409,184（约 400 MiB）；完成后 CPU 0%。不是连续采样的峰值，也不是长期性能保证。本例完整扫描及重复扫描均未复现旧 4.75 GiB 占用，支持释放边界修复有效；旧阻断在此复测场景通过，睡眠／重启／长时间持续采集的资源门槛仍开放。

当前完整 328 项回归通过，基准、Codex 与 DSH 三个实际本地测试均启用，专用 Debug 构建通过；正常 Release 仍待包含内存修复的最终更新。结束确认 QA 已退出、正常 PID 84482 保留，隔离备份／事实仅本地，不提交推送。仓库配置统计仍显示零仓库且独立提交未验收，不能以活动归属项目表替代 Git 发现／扫描；下一步核对这一实际边界及剩余声音／生命周期。

## 14:03 高内存候选修复与真实 DSH 单文件测量

针对上一轮 4.75 GiB 阻断，检查生产读取路径：压缩输出、事件批次和完整行已有容量限制，但长时间后台扫描没有明确分块的 Foundation 临时对象释放边界。候选修复将 scanQueue 设置 workItem autorelease 策略，JSONL 文件每个读取块、DSH 每个压缩读取块及 LineSplitter 每个完整行使用 autoreleasepool。保留检查点、错误传播、批次及去重逻辑；未改原日志或金额历史。此处为有待整体复测的解释，不能宣称内存增长根因已证明或所有资源问题已修复。

候选修复后原 327 项全量回归通过（真实副本与 Codex 日志启用）。随后增加显式 AIPULSE_QA_DSH_LOG 本地测试，并在独立筛选测试进程上解析一份 20,397,691 字节真实 DSH 日志临时副本：3,692 条事件、3.231 秒、每批最多 512、回调事件总数等于解析总数且返回值不保留事件数组。getrusage(RUSAGE_SELF) 输出自身峰值 RSS 从 30,982,144 到 39,337,984 字节（约 37.5 MiB），不是 swift 编译进程、整个应用或全部日志的峰值；没有未加池的同文件对照，不能单凭这个数证明因果。

新单项通过，测试副本结束删除，不作为 Git 素材；新增测试之后未重新执行完整 328 项，也未重建 Xcode QA。下一步重建当前源码并重复隔离真实扫描，重点检查扫描中／完成后内存，再恢复剩余切换；原 4.75 GiB 阻断仍开放。

## 13:56—13:59 实际本地采集与高内存阻断

本轮使用现有设置而非新增测试功能：重建当前通知文案的专用 QA，退出演示，通过目录选择器只为 QA 添加本项目目录。现行 pickDir 会显式调用 LogWatcher.start 与 triggerIngest；因此本轮 QA 不再是“未启动采集”的只读展示场景。读取真实本地日志，事件、检查点、书签和开发目录设置只写隔离域；ApiPoller 的 QA guard、Debug 云端写入禁用、全局静音及系统通知关闭保持不变。正常用户库／旧进程未改动。

实际从扫描中到“最近本地扫描已完成；覆盖仍限于可访问的支持日志”。最新只读检查：Codex 54,293 条（其中 33,439 有原始输出元数据）、DSH 17,619 条（全部有输出元数据）、Claude 9,825、Aider 18；460 个非零文件检查点，source＋dedupe_key 无重复，quick_check 为 ok。比基准新增及元数据补齐证明生产采集器实际工作，但尚未核对所有日志事件全集或重新扫描幂等；已有 gitmonitor_state 47 行是副本历史／派生状态，独立提交仍零，不能声称 Git 采集验收完成。

使用 computer-use 在实际采集交错期间完成首批 25 次三周期切换，逐次读取最新 AX、检查选中 Value 1 且无演示标记，失败零。后续三个汇总随采集更新为本周 485.7M、30 天 14.5G、今日 211.2M，时间平均各自独立。只有 25 次，不替代原 100 次门槛，自动等待的操作也不称亚秒压力测试。

发现明确资源阻断：QA PID 50833 运行 01:26 时 CPU 92.2%、RSS 687,968 KiB；03:22 时 CPU 17.5%、RSS 4,981,040 KiB（约 4.75 GiB），扫描已完成的界面仍可响应。此占用不能视为可接受稳定运行，原因尚未定位，不能只因扫描进度／无重复就关闭包 2 或 5。为避免继续消耗内存，已通过 QA 自身快捷键退出并确认 QA 不在运行、正常 PID 84482 保留；隔离采集库和配置保留仅本地用于诊断，不上传或删用户历史。下一步优先定位并修复该资源阻断，然后恢复持续采集与余下切换验收。

## 13:52 未运行来源提示与声音控件联动实测

重新构建最新十语言来源提示的专用 Debug QA，使用 computer-use 退出演示后，实际 AX 树显示“本地采集尚未运行；历史汇总不能证明当前活动为零”，同时保留真实副本今日 175.4M 汇总及分项覆盖说明。未启动采集器，因此只证明未运行状态展示，不证明扫描中、过期、失败或恢复的实际界面。

实际通知面板有独立系统通知、静音与声音免打扰、声音包及自动播放组。初始全局静音时三个试听按钮 disabled；解除静音后三个可用，重新静音后三个再次 disabled。静音时段关闭时两个时间输入 disabled，重新启用后可编辑；消费音效关闭时每小时限次步进器 disabled，重新启用后恢复。所有隔离测试开关恢复原值；系统通知始终关闭，未请求权限、改变系统设置或点击试听，声音播放、音质、自动事件及停止播放仍未验收。

界面发现旧“花费激增／异常”文案。核对 SpendAlertService 后确认激增检测仍基于观察到的余额净下降（不是词元估价），保留行为，仅把三条现行文案改为余额消费提醒、非逐笔账单及系统通知／声音独立控制，补齐十种语言。字符串 JSON、条目数与补丁检查通过；修改发生在实际 QA 运行之后，新文案尚未重新构建／实际观察。最近全量仍 327 项，不宣称本轮重跑。QA 已退出、正常 PID 84482 保留；隔离库 quick_check 为 ok，活动 68,435。没有改写原库或提交推送。

## 13:49 来源提示语言覆盖与最新 Release

五个 source.local_scan 提示各补齐十种语言（en、de、es、fr、ja、ko、pt-BR、zh-Hans、zh-Hant-HK、zh-Hant-TW）；JSON 结构检查确认每项十个本地化条目，英文及简体中文原文不变。此项只完成这些新增提示的语言覆盖，不声称所有现行界面的十语言人工排版已验收。

包含 UI 30 秒重评估及 Combine 导入的当前源码，真实基准／真实日志测试均启用，全量 327 项通过；正常 macOS Release／arm64／未签名构建退出码 0，JSON 与补丁检查通过。最近 Debug 为上一轮同源码状态逻辑，未包含本轮新增翻译；下一次专用 QA 构建须使用最新语言资源。未启动正常应用、改变音频、签名分发、提交或推送。新状态的实际未运行／过期／失败界面仍待运行验证，不将工作包 1 或 6 标完。

## 本地扫描运行状态收口（13:46）

最终正常 macOS Debug／arm64／未签名构建退出码 0，字符串 JSON 与补丁检查通过。定时器首轮构建因缺 Combine 显式导入失败，补导入后重新构建成功；没有把失败构建记为通过。没有启动新应用，Release 与新状态 UI 仍待验证。

增加 LogScanObservation 运行态，不写数据库历史或云端：未运行、扫描中、最近完成、过期、已记录的读写失败分开。LogWatcher 实际 runScan 开始／结束与 stop 更新状态，结束时间表示扫描循环返回，不是最新消费事件、所有目录可读或账户覆盖完整的证明。所有最近完成提示明确“覆盖仍限于可访问的支持日志”；已记录的 log.* 故障（大小写无关）优先显示失败，Git 故障不冒充日志故障。完成时间超过 120 秒或在未来判过期；仅重评估时间不刷新完成时间。界面在非演示模式、包括空数据页显示提示，并每 30 秒重评估，防止停止通知的采集器仍被显示为最近完成。

两项状态回归验证尚未扫描／停止不复用历史、开始扫描、完成、故障优先及过期／未来时间；真实副本和日志测试启用的全量 327 项通过。定时 UI 重评估随后加入，未单独重跑全量；正常 Debug 构建用于验证最终 UI。新增提示仅有英文／简体中文翻译，其他界面语言回退英语，完整语言覆盖及新状态实际运行仍待完成；不能据此将来源可用性整包标完。

## 真实顶部菜单一致性复核（模式修复后）

本轮不改源码、不重复构建，用 computer-use 启动上一轮当前源码专用 QA，退出演示后实际打开顶部 AI Pulse 菜单。菜单为“今日 · 175.4M 词元 · +0/-0 行 · 0 次提交”和“本周 · 446.9M 词元 · +0/-0 行 · 0 次提交”，与同一真实副本仪表盘一致，并有“一键静音”。选择今日并 Return 后实际回到今日 Value 1、175.4M，未出现演示数据。零提交仍受 QA 未配置开发目录／未启动采集的边界限制，不宣称用户真实成果为零。

尝试读取 com.apple.dock 两次均返回 computer-use timeoutReached，随后 QA 可继续观察并通过数据菜单返回仪表盘。因此 Dock 右键、菜单栏状态项及图标外围线仍未实际验收，不用顶部菜单或共用代码替代运行证据，也不因 Dock 观察超时判定应用冻结。QA PID 48397 运行 01:53 时单点 CPU 0.0%、RSS 208,640 KiB，非峰值、长期资源或持续采集性能证明。结束确认 QA 已退出、正常用户 PID 84482 保留；没有变更系统设置或正常库。

## 13:38—13:42 真实历史 UI 与模式切换修复

使用 computer-use 构建并实际操作当前源码的专用 QA；确认 QA 不在运行、旧隔离库活动／余额均零后先保留其备份，再把 13:30 的真实基准恢复到隔离库。最初 sandbox 写入被拒，检查仍为零后使用批准的精确路径恢复，quick_check 为 ok、68,435 条活动；正常库不变。QA 仍禁止真实采集、账户轮询及通知，Debug 云端写入禁用，不把此运行视为持续采集证明。

通过设置退出演示，真实今日 175.4M 词元／2 会话／1 活跃日，副本按现行词元 SQL 核对为 175,435,557；Codex 详情实际汇总 126.1M，Escape 关闭后回到仪表盘。真实本周 446.9M／5 会话／4 活跃日，均速 63.8M／日使用七天；30 天 11.3G／103 会话／29 活跃日，均速 376.8M／日。模型矩阵和缺少分项的覆盖说明均展示。QA 未配置开发目录，仓库／提交为零不构成仓库归属验收；详情未展开私人会话，不声称全部会话和轨迹已验证。

实际发现退出演示后首次切到 30 天闪现旧演示内存快照，稍后才恢复真实统计。修复为模式通知撤销三个周期请求、提升各自代次、清空旧快照／演示标记并重载三个周期；演示加载与初始 hydration 不使用真实派生缓存。重新构建专用 QA 后实际退出演示并切换今日／30 天／本周，均直接展示真实数据，未再观察到演示残留。全量 325 项通过、专用 Debug 构建及补丁检查通过；正常 Debug／Release 最新证据仍为此次修复前，需最终更新。

两次启动观察超时均以实际 PID 和同一 bundle ID 继续观察，未重复启动。结束仅退出 QA，确认 QA 不在运行而原 PID 84482 保留；隔离库 quick_check 为 ok，活动仍 68,435。数据仅本地保留，不上传私人素材。此轮补齐部分真实展示与模式切换证据，不关闭真实采集／全菜单 Dock／声音／生命周期工作包。

## 13:36 真实 Codex 中途检查点上下文恢复

将 LogWatcher 已有恢复逻辑提取为内部静态入口，正常扫描仍通过同一函数从自己的 filePositions 传入检查点。真实日志回归先完整读取，再在字节中段取得完整行检查点，丢弃解析器上下文后调用生产恢复函数读取后半段。后半段非零事件的去重键、仓库路径、模型和会话逐条与完整读取一致，最终游标相同，重复持久化不增加行或消费反馈。全量 325 项通过，未保存或输出会话内容，原日志与原库不变。

这是实际日志上的增量上下文组件验收，不是 FSEvents、持续追加、采集器重启或所有来源覆盖的运行证明。本轮只增加测试入口及回归，最新正常 Xcode 构建仍为 13:34、未包含此次入口提取；下一次实际 QA 构建必须使用当前源码。

## 13:34 真实 Codex 日志重放与当前构建

对本地一份当日真实 Codex rollout 日志建立临时快照，使用 CodexParser、JSONLCheckpoint 和 persistObservedEvents 生产组件在内存 SQLite 重放两次。实际回归通过：解析到非零事件、两次完整行游标一致、数据库行数不因重放增加、原始输出元数据及会话身份存在、未生成 cost_usd，统一总词元等于 input＋reported output（缓存与推理明细不重复相加）。设置明确 liveSince 截止，历史重放返回消费反馈零。原日志未修改，快照结束删除；固定测试 provider 占位不作为真实供应商归属验收。

显式本地启用真实日志和基准副本测试，全量 325 项通过，正常 macOS Debug 与 Release／arm64／未签名构建均退出码 0，补丁格式检查通过。未启动产物、提交、推送或发布。这更新当前源码编译门槛，并补充一份真实日志的组件重放证据；仍不能代替实际采集器的增量上下文恢复、多来源持续新增、界面及生命周期验收。

## 13:30 真实用户库只读基准与隔离副本

13:32 隔离迁移复核：增加内部 setup(at:defaults:) 入口，正常 setup() 仍使用原目录及 standard 偏好；测试只在临时副本与独立 UUID 偏好域运行同一生产启动流程。真实基准第一次迁移和第二次启动均通过，双向 EXCEPT 加行数检查证明 68,435 条活动的原 ID／时间／来源／模型／词元字段／历史金额／仓库／会话／去重键／金额归属未改变；允许已知供应商事实补全，provider_id 不在不变性检查内。代码事实的原 ID／提交哈希／时间／仓库／增删／合并标记、全部余额字段和原配额观察字段同样保留。新元数据列与独立提交表存在，尚无元数据的历史正确报告缺失，不制造完整覆盖。测试副本及 UUID 偏好域结束删除，基准副本保留，原库未修改。

新增 RealProfileMigrationTests 仅通过 AIPULSE_QA_BASELINE 显式本地启用；没有环境变量时跳过，不把私人数据库作为 Git 测试素材。它验证迁移保留与幂等，不验证日志重放、持续采集或实际界面；源码改变后旧 Debug／Release 构建结果不能替代最新构建。

原用户 Debug 数据库只读 `quick_check(1)` 返回 `ok`。使用 SQLite 在线 backup 保存一致性副本至 `/private/tmp/aipulse-real-data-qa.I00wsU/baseline.db`，副本检查同样为 `ok`；未启动新版正常应用、改写原库或替换正在使用的数据库。

副本含 68,435 条 usage_event：Codex 49,253、Claude Code 9,825、DeepSeek Harness 9,339、Aider 18。所有记录有 dedupe_key，按 source＋dedupe_key 分组未发现重复。Codex 最新时间为本地 2026-09-17 13:30:54，DeepSeek Harness 为 13:17:31；初次只读检查至副本完成，Codex 记录增加六条。这仅证明既有进程仍写入，不证明新版采集器无遗漏、恢复正确或持续运行通过。

当前原库仍没有 cache_creation_tokens、reported_output_tokens、reasoning_tokens 和独立 git_commit／git_commit_scan 表。因此旧字段求和不能作为新版完整词元统计的基准，旧 code_change 也不能代替提交事实。下一步在隔离副本上验证新版迁移前后原始记录保留及统计覆盖，再用于真实数据界面核对；不要直接以演示 QA 或旧进程替代新版验收。副本含私人历史，只用于本地验收，不加入 Git 或上传。

## 六包收口：最新 Release 构建与资源门槛

在减少动态效果及单次详情路径复用修改之后，重新运行正常 macOS Release／arm64／CODE_SIGNING_ALLOWED=NO 构建，退出码 0；本轮不修改源码，不启动产物。完成后以遇错立即退出的检查逐个比对九个 Sounds MP3 和 model-catalog.json，全部与本地一致；pricing-catalog.json 不存在，执行文件为 Mach-O arm64。git ls-files Sounds 只返回 README.md，九个 MP3 均由 check-ignore 确认排除。

这补齐当前源码构建／打包门槛，不补齐签名、实际播放、真实采集、生命周期或用户消费感验收。最近全量测试为 323 项通过，本轮没有重复运行单元测试；旧 Release 记录保留为历史，不再替代本次验证。

## 13:04—13:07 最新菜单与 Codex 详情复核

用 computer-use 操作重新构建的独立 QA 应用。顶部 AI Pulse 菜单实际显示今日 170.8K 词元／2 次提交、本周 1.2M／14 次提交及仓库成果；选择本周后仪表盘对应标签 Value 1，模型矩阵与仓库表总词元一致。从“查看工具详情”选择 Codex 后，详情明确为“演示汇总 · 无真实会话详情”，底层仪表盘不出现在详情 AX 树；Escape 后恢复本周仪表盘。

实际发现中文顶部标题仍为 File／Window，改用本地化键并补齐十个语言。退出旧 QA、重建后重新观察，真实 AX 树已显示“文件”“窗口”。最终 311 项测试、专用 Debug 构建、字符串 JSON 校验与补丁检查通过。启动观察超时后通过进程和同一 bundle ID 重读确认已启动，未将超时当作崩溃或重复启动。

这证明演示模式的入口、标签导航、关闭和中文菜单标题，不证明旧请求覆盖问题在真实操作中已经压力验收，也不证明真实会话、采集、声音、系统权限或长期运行。没有启动正常新应用、迁移用户数据库或连接真实供应商。

结束仅退出 QA，进程检查确认 QA 不在运行而原用户 PID 84482 保留。隔离数据库只读 quick_check 为 ok，usage_event 与 balance_snapshot 均为 0。

## 12:26—12:28 Release 编译与资源检查

运行正常 macOS Release 配置、arm64 目标、`CODE_SIGNING_ALLOWED=NO` 的 xcodebuild，退出码 0。无输出期间确认构建 handle 仍在运行、Swift 前端有编译 CPU 活动，没有把观察间隔解释为停止或重启构建。产物为 `AI Pulse.app`，Mach-O arm64，保留既有发布 bundle ID `com.wxy.aipulse`（此次没有创建新应用身份）。

产物 `Sounds/{coin,droplet,register}/{coin,coins,chime}.mp3` 九个文件逐个 cmp 与本地素材一致；九个本地 MP3 均被系统 afinfo 成功读取，时长约 0.3—3.2 秒。model-catalog.json 同样与本地一致，旧 pricing-catalog.json 不在包中。Git 仅跟踪 Sounds/README.md，音频仍被 ignore，不上传原始声音文件。修正播放解析注释中的旧 beep 回退描述，实际代码没有系统 beep 回退。

这是发布配置编译及打包完整性证据，不是声音听感、实际自动播放、Developer ID／商店签名、云端权限、历史迁移或 Release 实际运行验收。未启动该 Release 产物、未签发、未公证、未上传或发布，用户数据未改动。

## 12:24—12:26 遮罩键盘与辅助功能复核

将仪表盘与详情改为独立 ZStack 层，详情打开时底层禁用交互并 accessibilityHidden；详情关闭按钮使用 cancelAction 快捷键。更新专用 QA 构建后通过 `computer-use` 实际打开菜单，选择 Claude Code 并 Return 确认。详情的完整 AX 树只包含详情名称、排序、关闭、范围说明及窗口／菜单栏；底层三周期、图表、表格均不再暴露。

实际按 Escape 后详情消失，完整 AX 树恢复仪表盘；随后点击“本周”，Value 1、1.2M 词元及 169.2K 词元／日正确展示，证明关闭后底层交互恢复。最终 290 项测试和专用 Debug 构建通过，补丁检查通过。只退出 QA 后 `ps` 仍显示原用户 PID 84482，QA 已退出。

这项复核修正了上一节的 Escape／AX 问题，不扩大为 VoiceOver 全流程、真实会话／错误重试、声音、长期运行或整个 macOS 产品验收。

## 12:20—12:23 详情入口复核

使用 `computer-use` 在更新后的专用 QA 构建中实际打开“查看工具详情”菜单并选择 Claude Code。仓库眼睛已显示简短名称；菜单入口可发现且能打开详情。首次发现演示汇总与真实空数据库混用，显示零仓库／零词元／无会话。修订后重新构建、退出旧 QA 并启动新 QA，再次选择且按 Return 确认，界面明确显示“演示汇总 · 无真实会话详情”和配置真实工具的说明，演示模式不执行详情数据库查询。

完整 290 项回归通过，最终专用 Debug 构建通过。两次启动观察超时均未被解释为崩溃：第一次 `ps` 确认专用进程已启动，随后按独立 bundle ID 重新观察成功；第二次同样重新观察既有进程成功。结束仅退出 QA，原用户 PID 84482 仍运行；隔离数据库 quick_check 为 ok，活动／余额记录各 0。

边界：演示模式只验证入口和范围说明，未验证真实会话、轨迹、错误重试、声音或采集。按 Escape 后可访问性树无变化，因此**不能证明 Escape 关闭详情**，需要进一步修正和运行复核。可访问性树仍暴露遮罩后的仪表盘元素，也需检查键盘／辅助功能隔离；不能用截图上的覆盖效果替代验证。

## 身份与安全边界

- 用户原有进程 PID 84482，9 月 16 日启动，路径为 DerivedData 的 `AIPulse-aczbwitgdbvfhncfhiamhgkajsjm`；不能用它证明当前工作树运行结果。
- 最终专用进程 PID 97942，11:58 启动，bundle ID `xingyu.wang.aipulse.runtimeqa`，本地 ad-hoc 签名。
- 构建位于 `/private/tmp/aipulse-runtimeqa-products/AIPulseDebug.app`；独立偏好域，数据库为 `~/Library/Application Support/AIPulseRuntimeQA/aipulse.db`，不是用户 Debug 数据库。
- 专用身份仅在 Debug 下启用：演示模式、静音、跳过真实日志／Git 采集、通知授权与自动调度，云端写入由 Debug 策略禁止。仪表盘直接触发的余额轮询入口也跳过。
- 初次构建的 30 次切换发生在补齐直接轮询隔离之前，仅作为部分观察，不混入最终 100 次计数。未声称初次运行不存在凭据读取或网络尝试。

## 已验证

使用 `computer-use` 技能通过应用实际 UI 操作，每次读取最新 AX 状态后选择下一页并确认选中结果，不用源码推测 UI。

- 最终构建 100 次今日／本周／30 天交错切换，100 次均成功且存在周期头部，无观察到的冻结。
- 中途点击实际底部刷新入口一次，跨中午整点后，各周期更新自己的汇总。
- 刷新后：今日 144.5K 词元、5 会话、1 活跃日，均速 6.0K／小时；本周 1.2M、44 会话、4 活跃日，169.2K／日；30 天 10.1M、382 会话、30 活跃日，335.2K／日。数值来自明确标注的演示事实。
- 实际查看头像与分节相向节奏、工具模型矩阵、仓库表和带文字的工具详情入口。仓库环图仍用完整路径且截短，可读性需改进。
- SQLite 只读 `PRAGMA quick_check` 返回 `ok`，usage_event／balance_snapshot 均为 0；未将用户真实事实写入此库。
- 最终资源单次快照：QA RSS 120480 KiB（约 118 MiB）、CPU 0%；这不是峰值、长期泄漏或持续性能验收。
- 原进程 PID 84482 同时存活，未重启它。
- 最终代码完整回归 278 项通过，专用 Debug 构建和补丁格式检查通过。

## 未证明

切换操作约一秒一个，并未覆盖亚秒高频连击；仅一次手动刷新，也没有持续采集并发写入。不能据此宣称快速切换缺陷在真实工作流中完全修复。

尚待真实日志回读、用户库迁移与 Git 补扫、持续并发刷新、午夜／夏令时运行、睡眠唤醒、离线恢复、声音播放／停止、详情键盘与辅助功能、脉搏自然衰减、CloudKit 发布权限及签名 Release 验收。用户视觉与听感确认也未完成。

QA 应用会在检查后退出；临时应用与独立 QA 数据保留供复查，不自动删除用户文件，不提交、推送或发布。
